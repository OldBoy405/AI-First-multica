# CUSTOM.md — AI-First fork 二开台账

逐条记录本 fork 相对上游（`multica-ai/multica`）的所有定制。每次双周 rebase 前先核对本表，见 [CONTRIBUTING.AIFIRST.md](CONTRIBUTING.AIFIRST.md) 规则二、三。

**核对口径**（`// AIFIRST:` 单独一条不够用了——`.sql` 用 `-- AIFIRST:`，前端 `packages/` 一处标记都没有）：

```
grep -rnE "AIFIRST|CR-2026-[0-9]{3}" server/ packages/ --include=*.go --include=*.ts --include=*.tsx --include=*.sql --include=*.mjs | grep -v node_modules
```

**基线（2026-08-07，合并前）：156 个文件 / 452 处命中**，其中 `AIFIRST` 字样 84 处、`packages/` 下 0 处。合并后这三个数只能升不能降；降了就是挂钩点被上游覆盖掉了。前端不带标记，靠《前端定制文件清单》一节逐个文件核对。

## 合并冲突总则

执行 `git merge upstream/main` 时：

- **两边都保留**——不做「全取上游」或「全取二开」，如果功能重叠，在上游基础上，将二开新增的功能补齐。
- **逻辑整合**——以上游为基底，把二开改动贴回正确位置；上游改名则跟着改名。
- **加法优先**——二开以新增为主；未使用新能力时行为应与上游一致。
- **标记挂钩**——上游文件中所有 `AIFIRST` / `CR-2026-xxx` 标注的挂钩点逐条保留，按上游新代码结构贴回正确位置（上游改名则跟着改名）；合并后用上面的 grep 与本台账逐条核对，确认无丢失、无失效。
- **生成产物不手工 resolve**——`transitions_gen.go` / `gate_nodes_gen.go` / `pkg/db/generated/*.go` 的冲突一律**丢弃冲突块、重跑生成器**（前两个 `node gen/generate-*.mjs`，后者 `make sqlc`）。真源是 `tools` 仓 dir-graph / pipeline-templates 与 `server/pkg/db/queries/*.sql`——`generated/*.go` 里的 `// AIFIRST:` 注释是 sqlc 从 `.sql` 里搬过去的，不是手改，改 `.sql` 才有效。
- **减法定制是总则例外**——本台账只有 #1 是「摘除上游能力」。对这一行「两边都保留 / 加法优先」不适用，正确动作是在上游新基底上**重新摘除**。其余行一律按总则走。
- **合并后必跑**——`pnpm typecheck` + `pnpm test`（前端/TS），`make test`（Go）；省事可直接 `make check`，另按《已知测试失败基线》一节排除上游既有失败。逐行的最小验证命令写在《代码改动》表「合并注意」列末尾的「验证：」。

## 迁移编号冲突（已发生，不是「注意别撞」）

fork 的 9 个迁移占用 158–166，上游 `upstream/main` **这 9 个号全部已被占用**（上游已排到 169）：

| 号 | fork | upstream/main |
|---|---|---|
| 158 | `158_aifirst_cr_projection` | `158_agent_task_queue_chat_input_task_id` |
| 159 | `159_project_settings` | `159_chat_message_message_kind` |
| 160 | `160_issue_origin_project_chat` | `160_chat_message_input_owner_index` |
| 161 | `161_chat_session_project` | `161_agent_skill_enabled` |
| 162 | `162_aifirst_pipeline_runs` | `162_resource_labels` |
| 163 | `163_issue_origin_project_discussion` | `163_agent_builder` |
| 164 | `164_agent_task_queue_project_id` | `164_attachment_task_id` |
| 165 | `165_atq_project_active_index` | `165_attachment_task_id_index` |
| 166 | `166_project_presenter_grant` | `166_project_dates` |

**合并口径**：fork 的 9 个整体顺延到上游末位之后（170 起，保持相对顺序不变）。注意三件事：

1. **`160` / `163` 必须保持相对先后**——两者都用 `DROP CONSTRAINT + ADD CONSTRAINT` 重写 `issue_origin_type_check` 的整张枚举表。上游若也动了这个 CHECK，重编号后要把**上游的枚举值并进来**再加 `project_chat` / `project_discussion`，否则会把上游新 origin_type 静默删掉。
2. **`165` 是 `CREATE INDEX CONCURRENTLY` 单语句迁移**，不能与别的语句合并进同一文件（迁移器限制，见上游 068）。
3. **已应用的库要手工修 `schema_migrations`**——重编号不是改个文件名就完事：已部署环境按旧号记过版本，直接改名会导致新号重跑（`IF NOT EXISTS` 大多幂等，但 158/162 建表不是）或旧号残留。重编号后对每个已存在的库核对一次版本表。

历史先例：CR-2026-010 的三个迁移在 worktree 里原编号 161–163，合并进 main 时因与 CR-008/009 已占用的号撞上而顺延为 164–166——顺延本身是走通过的流程，不是新发明。

## 前端定制文件清单（`packages/`，无 `AIFIRST` 标记，靠本清单核对）

**fork 新增（合并时不会冲突，但要确认没被上游同名文件顶掉）：**

- `packages/core/projects/project-chat-store{,.test}.ts`
- `packages/views/projects/components/`：`surface-tab{,.test}.ts`、`project-chat-panel{,.test}.tsx`、`project-team-agent-chat{,.test}.tsx`、`project-queue-bar{,.test}.tsx`、`project-queue-status{,.test}.tsx`、`project-private-ask{,.test}.tsx`、`discussion-pane{,.test}.tsx`、`presenter-control-sheet{,.test}.tsx`、`cr-gate-card{,.test}.tsx`、`cr-status-badge{,.test}.tsx`

**fork 改上游既有文件（真正的冲突源，按冲突风险排序）：**

| 文件 | 归属 CR | 改动性质 |
|---|---|---|
| `views/chat/components/chat-input.tsx`{,`.test.tsx`} | 012 | **风险最高**：把上游组件拆成 `ChatInputCore` + `ChatInputDraftAdapter`，默认 adapter 回落 `useChatStore`。上游改这个文件几乎必冲突，且必须保住「传自定义 adapter 时不触碰 `useChatStore`」这条单测锁 |
| `core/api/client.ts` | 004/006/007/008/009/010/011/012 | 新增端点方法，取并集 |
| `core/api/schemas.ts` | 006/007/009/010/011/012 | 新增 zod schema，取并集 |
| `core/projects/queries.ts` | 004/006/007/008/009/010/011 | 新增 query hooks，取并集 |
| `core/projects/mutations.ts`{,`.test.tsx`} | 006/007/010/011/012 | 新增 mutation hooks，取并集 |
| `views/editor/content-editor.tsx`、`extensions/index.ts`、`extensions/mention-suggestion.tsx` | 012 | @提及限成员：上游改编辑器扩展装配时贴回 |
| `views/projects/components/project-detail.tsx` | 004/006 | 主区 Issues\|Chat tab 壳（`?tab=` 深链）；上游改 project 详情布局时贴回 |
| `views/inbox/components/inbox-page.tsx`、`inbox-detail-label.tsx`、`inbox-page-routing.test.ts` | 009/010 | 容器 Issue 跳转条 + presenter 通知类型路由 |
| `views/modals/quick-create-issue.tsx`{,`.test.tsx`} | 004 | 429 `project_queue_full` 分支 |
| `views/issues/components/issue-detail.tsx` | 010 | presenter 转移作为审计条渲染 |
| `views/chat/components/chat-message-list.tsx` | 006 | 群聊复用消息流 |
| `core/realtime/use-realtime-sync.ts` | 004/011 | 新 WS 事件订阅 |
| `core/types/{issue,project,agent,inbox,events}.ts` | 004/006/009/010/011/012 | 新字段/新枚举值，取并集 |
| `views/locales/{en,ja,ko,zh-Hans}/{projects,inbox,modals}.json` | 004/006~012 | **四语言必须同步**，新增键取并集；漏一种语言 typecheck 不报、运行时掉 key |

## 合并官方记录（摘要）

| 日期 | 对齐 | 冲突要点 |
|---|---|---|

## 代码改动

治理链路（#1–#16）与 P2 三模式聊天（#17–#25）两条线。

| # | 位置 | 改动 | 原因 / 追溯 | 日期 | 合并注意 |
|---|---|---|---|---|---|
| 1 | `server/cmd/server/router.go`（2 处，均有 `// AIFIRST:` 标记） | 摘除 Stripe webhook（`/api/webhooks/stripe`）与 `/api/cloud-billing` 路由组的挂载；handler 代码保留未删 | 内网自托管无云端计费；访问即 404。CR-2026-001 FR-1（AI First Platform 仓库 `change-requests/CR-2026-001/`） | 2026-07-30 | **⚠ 总则例外：本行是减法定制**，不适用「两边都保留 / 加法优先」。上游改 Stripe／cloud-billing 挂载代码时以上游为基底**重新摘除**挂载（handler 保留）；路由组改名则跟改，两处 `// AIFIRST:` 标记保留。验证：起服务后 `curl -i .../api/webhooks/stripe` 与 `.../api/cloud-billing` 均须 404 |
| 2 | `server/pkg/agent/claude.go#isFilteredChildEnvKey`（`// AIFIRST:` 标记） | 过滤名单补入 `CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST` 与 `CLAUDE_CODE_HOST_AUTH_ENV_VAR` 两个宿主托管认证标记 | 二分实证：两者任一单独泄漏给 daemon 拉起的 claude 子进程即报 `Not logged in`（凭据文件明明有效）；同批的 CHILD_SESSION/HOST_SESSION_ID/SDK_HAS_*_REFRESH 单独存在无害、未动。**候选回馈上游 PR**（上游注释明说该名单按名维护、欢迎补全）。CR-2026-001 TASK-04 | 2026-07-31 | 上游在过滤名单新增条目时两边都保留；函数改名则跟改。若上游已收编这两条目，摘除挂钩并在台账标记已回馈上游。验证：`cd server && go test ./pkg/agent/ -run MergeEnvFiltersClaudeCodeVars -v` |
| 3 | `server/migrations/158_aifirst_cr_projection.{up,down}.sql`（新文件） | 新增治理投影三表：`cr` / `cr_sync_event` / `approval_record`（approve 用部分唯一索引 `WHERE decision='approve'`，reject 多条留痕） | P0 数据模型映射（git 权威 / PG 投影）落地；表可清空重放。CR-2026-002 TASK-04 | 2026-07-31 | **已与上游 158 撞号**，按《迁移编号冲突》一节整体顺延；本迁移是建表（非幂等），重编号后必须核对已应用库的 `schema_migrations`。验证：`make migrate-up` 后 `\d cr` 三表在 |
| 4 | `server/internal/governance/`（新包：`actions.go`、`transitions_gen.go`、`gen/generate-transitions.mjs`、测试） | 治理层自研包：activity_log 两个 `aifirst.` action 常量 + CR 状态机只读副本（**25 条声明 / 47 条展开**，生成产物入库，`gen --check` 守一致性）+ 后续 crsync/approval/reconcile 落此包 | 规则一（自研代码住新目录）；构建不依赖 tools checkout（SDD-SUG-003）。状态机变更流程：改 tools dir-graph.yaml → 重跑 gen → 提交两仓。CR-2026-002 TASK-04；**转移数于 CR-2026-022 由 45 展开条更新为 25 声明 / 47 展开**（补入 approve/reject/rollback 转移） | 2026-07-31（数量 2026-08-05 更新） | 自研新包无冲突；`transitions_gen.go` 是生成产物，冲突丢弃后重跑生成器。关注上游对 activity_log 表结构／events.Bus 的改动，确保 `aifirst.*` 写入与 `cr:updated` 广播仍适配。验证：`node server/internal/governance/gen/generate-transitions.mjs --check` + `cd server && go test ./internal/governance/ -run 'TransitionTableShape\|IsLegalTransition\|ActionConstants' -v` |
| 5 | `server/internal/governance/crsync.go` + `server/cmd/server/router.go`（1 处 AIFIRST 挂载：`POST /api/daemon/cr-events`） | CR 投影 worker：事件幂等入账（`ON CONFLICT DO NOTHING`）→ per-CR 互斥 → 合法转移校验（transitions_gen）→ 更新 cr 行 / 乱序置 needs_reconcile → `cr:updated` 经 events.Bus 自动广播 workspace 房间。workspace 绑定只信 DaemonAuth 上下文，请求体 workspace_root_hash 仅日志用 | P1 §A 同步协议服务端半边；**治理包内**直接用 pgx 不走 sqlc（避免动上游 query 文件）——注意此约束只覆盖 governance 包，P2 聊天线（#17 起）确实改了 `queries/*.sql`。CR-2026-002 TASK-05 | 2026-07-31 | router.go daemon 组挂载点保留；上游重构路由则把该端点贴回新位置。events.Bus 广播随上游事件机制改名则跟改。验证：`cd server && go test ./internal/governance/ -run 'LegalFlowProjectsAndBroadcasts\|DualChannelSameEvent\|OutOfOrderFlagsReconcile\|IllegalTransitionFlagsReconcile' -v`（需真库，见 C6） |
| 6 | `server/internal/daemon/crevents.go`（新文件）+ `daemon.go`（1 处 AIFIRST 启动钩子）+ `config.go`（CRWorkspaceRoots 字段 + `MULTICA_CR_WORKSPACES` env）+ `client.go` 不动（ReportCREvents 定义在 crevents.go 里） | daemon CR 事件采集器：heartbeat 同周期扫 `{root}/.crctl/outbox/`（主）+ commit 兜底扫描（`.scan-cursor` 游标，四类 `[cr] ` 前缀契约）→ 双通道按幂等键合并（outbox 赢）→ 批 ≤100 上报 → 仅 accepted 删文件、rejected 三振进 `outbox/dead/`、上报失败游标不动（离线积压重试） | P1 §A.3。**部署口径**：daemon 启动环境需设 `MULTICA_CR_WORKSPACES=<knowledge-base 本地路径>`（分号分隔多工作区），未设则采集器完全关闭。commit 扫描的 git 调用暂直调 exec（只读 log/rev-parse），TASK-09 gitguard 收编（代码内 TODO）。CR-2026-002 TASK-06 | 2026-07-31 | daemon.go 启动钩子保留；上游改 daemon 初始化／heartbeat 结构则把钩子贴回新位置。上游给 heartbeat 加新逻辑时两边都保留。验证：`cd server && go test ./internal/daemon/ -run 'DualChannelMergedWithoutDuplicates\|PartialAcceptDeletesOnlyAckedFiles\|PoisonedFileQuarantined\|NetworkFailureKeepsBacklog\|ParseCRCommitMessage' -v` |
| 7 | `server/internal/governance/approval.go`（新）+ `crsync.go`（evidence 持久化）+ `crevents.go`（grants 轮询落盘 `.crctl/grants/`）+ `router.go`（2 处 AIFIRST 挂载：用户组 `/api/workspaces/{wid}/crs*` 三端点、daemon 组 `approvals/pending\|ack`）+ **迁移 158 内联增列**（cr_sync_event.evidence、approval_record.workspace_id/grant_json/delivered_at） | Ed25519 签名审批：`RequireHumanActor`（task_token 403）→ 证据漂移比对（409）→ 签发 grant（canonical 与 crctl 完全一致，共享向量锁定）→ approval_record 幂等落库（approve 部分唯一索引；重批返回首次 grant）→ daemon 队列 pending/ack 投递。私钥仅 `APPROVAL_SIGNING_KEY`（base64 PKCS#8）内存驻留，未配置=端点不挂载，配置无效=拒绝启动，日志只出 key_id | P1 §B。**部署口径**：`APPROVAL_SIGNING_KEY` + `APPROVAL_SIGNING_KEY_ID` 成对配置；公钥（`PublicKeyPEM()` 输出）提交进 knowledge-base 仓 `.crctl/keys/{key_id}.pub`。**跨工具接缝测试**：Go 签发 grant → 真实 crctl `approve --grant` 验签放行（`approval_crosscheck_test.go`，需 CRCTL_PATH）。CR-2026-002 TASK-08 | 2026-07-31 | router.go 两处挂载保留；上游改鉴权／路由组结构则在新基底贴回。**历史事实**：这批列是直接改进 158 号迁移文件的（当时 158 尚未在任何长期库上应用）；**今后一律新开顺延迁移，勿再手改 158**。验证：`cd server && go test ./internal/governance/ -run 'CanonicalDigestParity\|SigningKeyLoading\|Approve\|GrantDeliveryQueue\|ApprovalCardDoesNotLeak' -v`；跨工具接缝另跑 `CRCTL_PATH=<crctl 路径> go test ./internal/governance/ -run GrantCrossVerifiesWithCrctl -v` |
| 8 | `server/pkg/gitguard/`（新包）+ `cmd/multica/cmd_gitguard.go`（隐藏子命令 `gitguard-exec`）+ `internal/daemon/execenv/crguard_config.go`（新，PATH shim + Claude hooks 铸造）+ execenv `git.go`/`execenv.go`/`runtime_config_sections.go`（AIFIRST 挂钩：自守/shim 接线/上下文注入）+ `daemon.go`（PATH 前插 + CRCTL_WORKSPACE）+ `crevents.go`（TASK-06 TODO 收编，git 过闸） | controlled-shell 下沉：rules.json 的 Go 消费方（三元白名单 Check/Run）；每任务铸造 `{envRoot}/bin/git(.cmd)` shim 经 `multica gitguard-exec` 过闸；daemon 自身 worktree 操作 caller=system-orchestrator；Claude 后端自动物化 `.claude/settings.json` PreToolUse hooks；Agent 上下文注入 git 约束段 | P1 §C。**部署口径**：`MULTICA_CONTROLLED_SHELL_RULES=<rules.json 绝对路径>`，未设=不启用（上游行为不变），配置无效=fail-closed 拒绝一切 git。**层级**：L2 PATH shim（绝对路径可绕，文档明示）+ L3 IDE hooks（Claude）；**L1 --disallowedTools Bash 未接**（agent permission.bash 平台不持久化，无可信键；见任务完成记录）。**跨引擎修复**：rules.json 的 `add` 形态负向先行断言改 RE2 兼容（tools@4907539），Go RE2 不支持 `(?!)`。CR-2026-002 TASK-09 | 2026-07-31 | execenv 被上游重构时三处 AIFIRST 挂钩贴回新结构；daemon.go PATH 拼装跟上游。未配 `MULTICA_CONTROLLED_SHELL_RULES` 时行为必须与上游一致（加法优先验证点）。验证：`cd server && go test ./pkg/gitguard/ ./internal/daemon/execenv/ -v`（`TestCheckTable`/`TestRealRulesLoadUnderRE2`/`TestFromEnvSemantics`），并**不设** `MULTICA_CONTROLLED_SHELL_RULES` 再跑一次 execenv 测试确认与上游同行为 |
| 9 | `server/internal/governance/audit.go`、`toolcalls.go`（新）+ `crsync.go`（audit 事件分流 3 处小改）+ `server/pkg/gitguard/spool.go`（新）+ `cmd/multica/cmd_gitguard.go`（拒绝路径 1 处接线）+ `internal/handler/daemon.go`（CompleteTask 2 处 AIFIRST 钩子） | AI 行为审计（零新增探针）：① gitguard-exec 拒绝 → `SpoolDenial` 写 outbox audit 事件（`{caller, sub, code}`，无参数正文）；② crctl gate/validate 漂移检出 → 同通道 audit 事件（摘要对，无证据内容）；服务端 `audit` 类事件绕过 cr_sync_event 账本直插 `activity_log`（action 白名单锁死两个 `aifirst.*` 常量，防伪造）；③ CompleteTask 从已持久化 task_message 流聚合工具调用摘要（工具名/目标路径/结果态，封顶 200 条）进 result JSONB | P1 §C.5/§B.4。**留证语义**：audit 无幂等键（无 commit sha），追加型，采集 ack 崩溃窗可能重复——审计计数可接受；同一漂移待采集期由确定性文件名去重。CR-2026-002 TASK-10 | 2026-07-31 | handler/daemon.go CompleteTask 两处钩子保留；上游改 CompleteTask 流程则贴回新结构，摘要聚合基于上游的 task_message 持久化流。cmd_gitguard.go 拒绝接线跟子命令重构走。验证：`cd server && go test ./internal/governance/ -run 'Audit\|SummarizeToolCalls' -v` + `go test ./internal/handler/ -run AggregatesToolCallSummary -v` + `go test ./pkg/gitguard/ -run SpoolDenialWritesOutboxEvent -v` |
| 10 | `server/internal/governance/reconcile.go`、`reconcile_github.go`（新）+ `crsync.go`（snapshot 分流）+ `cmd/server/main.go`（1 处 AIFIRST：sys_cron 任务注册）+ `internal/daemon/crevents.go`（快照上报 + `.crctl` 目录缺失时游标写失败的既有边角修复） | reconcile 对账双模式（git 权威、只读、不反向写 git）：server 模式 = sys_cron 任务（默认 5min）经 GitHub API 拉 origin HEAD + raw `_backlog.yml`；daemon 模式 = daemon 每 5min 把 {HEAD, raw backlog} 作为 `snapshot` 事件走既有 cr-events 通道。两模式汇聚 `ApplySnapshot`（单实现）：漂移覆写、needs_reconcile 清除、缺行补插、快照外行不动、未知状态永不投影 | P1 §A.4。**部署口径**：server 模式设 `REMOTE_RECONCILE_MODE=server` + `KNOWLEDGE_BASE_REMOTE_URL`（https://github.com/{owner}/{repo}）+ `KNOWLEDGE_BASE_TOKEN`（fine-grained 单仓 Contents:Read-only PAT，永不入日志）+ 可选 `RECONCILE_INTERVAL`；配置坏 = 拒绝启动。daemon 模式零配置（采集器开即带）。**新环境部署顺序**：先起 daemon 通道建立投影首行，再依赖 server 对账接管——server 模式不自举空投影（服务端不猜 workspace 绑定，设计约束）。多知识库需 workspace→remote 映射表（代码内 ponytail 注记）。CR-2026-002 TASK-07 | 2026-07-31 | main.go sys_cron 注册为 AIFIRST 挂钩；上游改任务调度机制则贴回新位置。快照继续走 cr-events 通道，上游加新事件类别时两边都保留。验证：`cd server && go test ./internal/governance/ -run 'ParseBacklog\|ApplySnapshot\|SnapshotEventHeals' -v` |
| 11 | `server/internal/governance/daemon_workspace.go`（新）+ `crsync.go`/`approval.go`（3 处绑定点改走 `resolveDaemonWorkspace`）+ 测试 | daemon workspace 绑定缺陷修复：原设计只认 `mdt_` daemon-token 绑定，但上游 `mdt_` 签发流程尚未接线（`auth.GenerateDaemonToken` 无调用方），真实 daemon 走 PAT 回退路径 → 治理三端点全 403。改为上游同款约定：`mdt_` 绑定优先；PAT 回退按服务端置的 `X-User-ID` 查 `member` 表绑定（单成员隐式、多成员需 `X-Workspace-ID`、非成员 403），请求体始终不可信 | CR-2026-002 TASK-11 实测暴露。此前 T05 集成测试用 `WithDaemonContext` 直接注入绕过了该路径，故单测全绿但真机 403。CR-2026-002 TASK-11 | 2026-07-31 | 关注上游是否为 `auth.GenerateDaemonToken` 接线或改 daemon PAT 认证路径：若有，复核 `resolveDaemonWorkspace` 三处绑定点是否仍成立；新包本身无冲突。验证：`cd server && go test ./internal/governance/ -run 'PATFallbackBindsSingleMembership\|PATFallbackRejectsOutsiders' -v`，外加真机跑一次 daemon 确认治理三端点不 403 |
| 12 | `server/migrations/162_aifirst_pipeline_runs.{up,down}.sql`（新文件）+ `server/pkg/db/queries/agent.sql`（`CreateRetryTask` 克隆列表补 `cr_id`/`pipeline_node_run_id`） | 新增 pipeline runner 状态投影两表：`pipeline_run` / `pipeline_node_run`（`detail` JSONB 是对 P0 §3.4 的单列增补，存 review 节点的 blocker 明细）；`agent_task_queue` 增 `cr_id`/`pipeline_node_run_id` 两列（B4）。两列由 StartTask 回调后置写入，`ClaimAgentTask` 序列化谓词与"四 FK 全空"互斥类不感知新列，无需改动 | P2 三模式聊天 CR-F（D7 门禁接合）；P0 §2.2/§3.4。首期写者是治理层门禁节点投影器（尚无 Runner）。CR-2026-011 TASK-01。**编号更正**：在 worktree 里原为 161，合并进 main 时因与 CR-008 撞号已顺延为 162 | 2026-08-02 | **已与上游 162 撞号**，按《迁移编号冲突》一节整体顺延（建表非幂等，须核对 `schema_migrations`）。`agent_task_queue` 上游新增列时两边都保留，注意 Clone 列清单与 `ClaimAgentTask` 谓词对新列的感知。验证：`make sqlc` 后 `git diff --stat server/pkg/db/generated/` 只应含预期文件 |
| 13 | `server/internal/governance/gate_nodes_gen.go`（新，生成产物）+ `gen/generate-gate-nodes.mjs`（新生成器）+ `gate_projection.go`（新）+ `crsync.go`（`applyStatus` 两处挂点：fresh-insert 分支与合法转移分支各接入 `projectGateTransition`） | 门禁节点投影器：从 crctl 状态事件流投影 `pipeline_run`/`pipeline_node_run`（仅 4 个 human_approval 门禁节点，尚无 Runner）。节点 UUID **不派生**——核实 tools pipeline-templates 的每个节点已带固定 id，生成器直接抽取（`gen --check` 守一致性，同 `transitions_gen.go` 模式），这就是 TSUG-002 要求的"单一决议点"。投影失败静默降级（DD-2：审批卡可见性只依赖 `cr.status`，不依赖本表）；`started_by` 无真实 actor，回退到 workspace 最早 owner | P2 三模式聊天 CR-F（D7 门禁接合）SDD DD-1/DD-6。CR-2026-011 TASK-02 | 2026-08-02 | 上游改 `crsync.go` `applyStatus` 结构时，fresh-insert 与合法转移两分支重新贴回 `projectGateTransition` 挂点；`gate_nodes_gen.go` 是生成产物，冲突丢弃后重跑生成器。验证：`node server/internal/governance/gen/generate-gate-nodes.mjs --check` + `cd server && go test ./internal/governance/ -run GateProjection -v` |
| 14 | `server/internal/daemon/crevents.go`（第 5 类 commit 正则 `crCommitReviewRe` + `buildReviewPayload`）+ `crsync.go`（`knownEventKinds` 增 `review` + `apply` 分派）+ `gate_projection.go`（新 `applyReview`） | review 事件通道：blocked/passed 评审对平台可见（此前无 advance 就无 outbox 事件，平台完全看不到 block）。`review-annotations/{stage}.yml` 读取方式是**工作树直读而非 `git show {sha}:path`**——已核实 controlled-shell `rules.json` 的 git 白名单不含 `show` 子命令，加它是 tools 仓的跨仓改动，超出本 CR 范围；`ponytail:` 标注了这一简化的天花板（daemon 扫描游标跨整轮 reviewLoop 才会失真）与升级路径。tools/crctl **零改动** | P2 三模式聊天 CR-F SDD DD-3。CR-2026-011 TASK-03 | 2026-08-02 | 上游在 commit 扫描／事件分派处新增能力时两边都保留，`knownEventKinds` 补回 `review`；新正则与 `buildReviewPayload` 为新增，与上游能力并排贴回。验证：`cd server && go test ./internal/governance/ -run ApplyReview -v` + `go test ./internal/daemon/ -run ParseCRCommitMessageReviewContract -v` |
| 15 | `server/internal/governance/project_gates.go`（新，`HandleProjectGates` + `canApprove` + `pendingApprovalStage`）+ `router.go`（`/api/projects/{id}/gates`，同一 `approvalSvc` 特性开关，挂在既有 project 路由组内不新写鉴权）+ `approval.go`（`HandleApprove` 增 `canApprove` 调用，403 `FORBIDDEN_APPROVER`）+ `pkg/db/queries/agent.sql`（`SetTaskCRAttributionIfValid`）+ `service/task.go`（`AttributeTaskToCR`）+ `handler/daemon.go`（`StartTask` 接受可选 `cr_id` 自报） | ① 项目群聊窗口读门禁数据的唯一入口：16 态 + 待批段 + 审批卡数据 + `pending_advance`（已批准待 crctl 推进的服务端派生态）+ 门禁节点历史；② `HandleApprove` 补角色策略——此前只查人类身份，任何工作区成员皆可批准，现收紧为 owner/admin（`cr.owners` 对应角色因无 crctl `--caller` 到 Multica 用户的身份桥接，`ponytail:` 标注暂缓，见 `canApprove` 注释）；③ `agent_task_queue.cr_id` 归因：daemon 从任务 workdir 分支名自报，服务端按调用方工作区校验后才落列，不信任自报内容 | P2 三模式聊天 CR-F SDD §4.3/§4.4/§4.5/DD-4/DD-5。**顺带修复**：`approval_test.go`/`resetCR` 缺 `approval_record` 清理，导致该测试在非临时（本地持久）数据库上二次运行必挂——预存的旧签名验不过新一次运行的公钥；已补清理，与本 CR 逻辑无关但在本任务测试中暴露。CR-2026-011 TASK-04 | 2026-08-02 | 上游改 project 路由组时把 gates 端点贴回组内新位置；上游改 `HandleApprove` 时在其上叠加 `canApprove` 角色策略，勿覆盖；上游改 `StartTask` 时补回可选 `cr_id` 自报参数；`queries/agent.sql` 改后 `make sqlc` 重生成。验证：`cd server && go test ./internal/governance/ -run 'ProjectGates\|ApproveRejectsNonOwnerAdmin' -v` + `go test ./cmd/server/ -run SetTaskCRAttributionIfValid -v` |
| 16 | `server/internal/daemon/crevents.go`+测试、`governance/crsync.go`、`reconcile.go`、`reconcile_github.go` | 治理核心两处缺陷修补：① `projectableSha` 拦住占位符 sha 进投影指针——`--embedded` 模式下 `commit_sha` 为空串使幂等键 `(cr_id, commit_sha, event_kind)` 互撞，同一 CR 连续两次 embedded advance 的第二条 status 事件被 `ON CONFLICT DO NOTHING` 静默丢弃；② reconcile 只读 `_backlog.yml`，归档 CR 已移进 `_history.yml` 而永久失去自愈能力——新增 `ParseHistory`/`mergeAuthority`，server/daemon 双模式都把 `_history.yml` 并入权威快照，向后兼容 | CR-2026-002 归档收尾核对发现（CR-2026-001/002 当时卡在 `writing-back` + `needs_reconcile=true`，与真实 archived 不符）。CR-2026-003 TASK-01/02 | 2026-07-31 | 全部落在 fork 自有文件内，无上游冲突面。合并后确认 `_history.yml` 解析未被上游的 YAML 依赖升级破坏。验证：`cd server && go test ./internal/governance/ -run 'ParseHistory\|EmbeddedPlaceholderEventsDoNotCollide\|CheckpointFillsProjectedCommitForEmbedded\|SnapshotEventHeals' -v` |
| 17 | 迁移 `159_project_settings`（新）+ `server/internal/service/task_queue_capacity.go`（新）+ `service/task.go`（软上限语义）+ `handler/issue.go`（`writeProjectQueueFull` + 429 分支）+ `handler/project.go`（PATCH settings 白名单校验，`team_agent_queue_limit`）+ `handler/chat.go`、`handler/cancel_task_by_user_test.go`+ `pkg/db/queries/{agent,project}.sql` → `make sqlc` | Team Agent 共享队列容量治理（D1）：队列达上限（默认 50，per-project 可配）时非 owner/admin 入队拒 429 `project_queue_full`；owner/admin 不受限且 claim 时优先；用户可撤回自己的排队项（软删除留审计痕迹），槽位释放走既有 WS 通道广播 | P2 三模式聊天设计 §11 交付切分 D1。CR-2026-004 | 2026-08-01 | `159` 已与上游撞号（见《迁移编号冲突》）。`handler/issue.go` 的 429 分支挂在上游 quick-create 路径上——上游改这段错误处理时把 `writeProjectQueueFull` 分支贴回；`handler/project.go` 的 settings 白名单要与上游新增的 settings 键取并集（**别把上游新键挡掉**）。验证：`cd server && go test ./internal/handler/ -run 'ProjectQueueCapacity\|CancelTaskByUser' -v` + `pnpm -C packages/views vitest run modals/quick-create-issue.test.tsx` |
| 18 | 迁移 `160_issue_origin_project_chat`（新）+ `server/internal/service/project_chat.go`（新）+ `handler/project_chat.go`（新）+ `handler/issue.go`（三处列表/看板/搜索的 `origin_type IS DISTINCT FROM 'project_chat'` 隐藏过滤）+ `handler/project.go`、`pkg/db/queries/{issue,project}.sql` → `make sqlc`；前端见《前端定制文件清单》 | 三 tab 项目群聊窗口（D2 骨架 + D3 核心）：`project-detail` 主区新增 Issues\|Chat tab（`?tab=` 深链）、Chat 内三模式 tab 条、草稿按 `{projectId}:{mode}` 独立持久化；Team Agent 面走「隐藏容器 Issue + 薄发送端点」——容量守卫 → 落 comment → enqueue，满队同步 429 且评论不落库；消息渲染复用 issue-timeline/CommentCard/TimelineView | P2 三模式聊天交付切分 v2 CR-A。CR-2026-006 | 2026-08-01 | **`160` 撞号 + CHECK 约束整表重写**：`issue_origin_type_check` 是 DROP+ADD 全量枚举，上游若也动了这个 CHECK，必须把上游枚举值并进来再加 `project_chat`。`handler/issue.go` 的三处隐藏过滤是**容器 Issue 不外泄的唯一防线**，上游改列表/看板/搜索查询时逐处贴回，漏一处容器 Issue 就会出现在用户列表里。验证：`cd server && go test ./internal/handler/ -run 'ProjectTeamAgentID\|GetProjectDiscussion\|DiscussionContainerExcluded' -v` + `pnpm -C packages/views vitest run projects/components/project-chat-panel.test.tsx projects/components/project-team-agent-chat.test.tsx` |
| 19 | 后端仅 `handler/agent.go`（DD-1 发起人链路字段）+ `handler/cancel_task_by_user.go`（发起人永远可撤回自己任务的 fast-path 重排）+ `handler/chat.go`；其余为前端（`project-queue-bar`、`project-queue-status` 新增；`core/projects/{queries,mutations}.ts`、`core/types/agent.ts` 改） | D3 完整形态：队列条常驻「N 条排队」+ 展开逐项显示发起人 + 发起人撤回（403/409 结构化反馈）；停止（发送者停自己 / owner 停任意）；「只看 Agent 请求」本地过滤开关；消息悬浮仅复制。队列条经 WS 实时更新无需手动刷新 | P2 三模式聊天交付切分 v2 CR-B（纯前端为主，后端能力复用 CR-2026-004）。CR-2026-007 | 2026-08-02 | `cancel_task_by_user.go` 的改动是**权限判定顺序重排**（发起人 fast-path 先行），不是新增分支——上游改这段权限逻辑时不能简单取并集，要重新确认「发起人可撤回自己 + owner 可撤回任意」两条同时成立。验证：`cd server && go test ./internal/handler/ -run CancelTaskByUser -v`（`PrivateAgent_Originator_Succeeds` / `PrivateAgent_NonOriginatorMember_Returns403` / `OwnerCancelsAny` 三条必须同时绿）+ `pnpm -C packages/views vitest run projects/components/project-queue-bar.test.tsx` |
| 20 | 迁移 `161_chat_session_project`（新）+ `pkg/db/queries/chat.sql` → `make sqlc` + `handler/handler.go`、`handler/agent.go`、`handler/daemon.go`、`cmd/server/listeners.go`（per-user 推送）+ `internal/daemon/{daemon,health,types}.go`、`execenv/{execenv,runtime_config_sections}.go`（ask-only 强制）+ `cmd/multica/cmd_repo.go`；前端 `project-private-ask.tsx`（新） | Private Ask 面（D5）：`chat_session` 加 nullable `project_id`，按 `(project_id, creator_id)` 查会话，与既有全局 1:1 chat 并存；前端绕开 `use-chat-controller.ts`（耦合全局 `useChatStore` 单例），直接组合纯 props 的 ChatMessageList + ChatInput + TaskStatusPill；语义四差异：个人独立队列、**默认 Ask-only（execenv 不给写权限）**、仅本人可见、与 Team Agent 并行 | P2 三模式聊天交付切分 v2 CR-C。CR-2026-008 | 2026-08-02 | **隐私红线**：单 socket 架构下 Private Ask 消息不进全工作区广播，靠 `cmd/server/listeners.go` 的服务端 per-user 推送保证（前端过滤不算数）。上游改事件分发/监听器装配时必须重新验证这条，`listeners_scope_test.go` 是它的锁。ask-only 由 execenv 的 runtime config 段落实现，上游改 execenv 段落装配时贴回。`161` 撞号。验证：`cd server && go test ./cmd/server/ -run RegisterListeners -v` + `go test ./internal/handler/ -run 'GetProjectPrivateChat\|PrivateAskSessionExcludedFromGlobalChat' -v` + `go test ./internal/daemon/execenv/ -run 'ClassifyTask\|SlimKindMatrix' -v` |
| 21 | 迁移 `163_issue_origin_project_discussion`（新）+ `handler/issue.go`（隐藏过滤补 `project_discussion`）+ `handler/comment.go`（触发豁免：Discussion 评论不产生 agent 任务）+ `pkg/db/queries/{issue,project}.sql` → `make sqlc`；前端 `discussion-pane.tsx`（新）+ `inbox-page.tsx`/`inbox-detail-label.tsx`（容器跳转条） | Discussion 面（D6）：第二个隐藏容器 Issue（`origin_type='project_discussion'`，首次打开时 lazy 创建），纯人类多人聊天，复用 comment-card/reply-input/@提及/通知/订阅基建；输入区无模式/模型下拉、无上下文用量，只留 @ | P2 三模式聊天交付切分 v2 CR-D。CR-2026-009 | 2026-08-02 | 同 #18 的两条：**`163` 撞号 + CHECK 约束整表重写**（枚举现含 `project_chat` 与 `project_discussion` 两值，重编号后须保持在 160 之后）；`handler/issue.go` 隐藏过滤逐处贴回。**验收红线**：Discussion 消息不得产生任何 `agent_task_queue` 行（除非 #25 的 DC 被显式激活）——`handler/comment.go` 的触发豁免是唯一实现点，上游改评论触发逻辑时必须贴回。验证：`cd server && go test ./internal/handler/ -run 'DiscussionContainerExcluded\|ComputeCommentAgentTriggers_DiscussionContainer' -v` + `pnpm -C packages/views vitest run projects/components/discussion-pane.test.tsx` |
| 22 | 迁移 `164_agent_task_queue_project_id` / `165_atq_project_active_index`（CONCURRENTLY 单语句）/ `166_project_presenter_grant`（新）+ `pkg/db/queries/{agent,project_presenter}.sql` → `make sqlc`（`generated/agent.sql.go` 4 处 AIFIRST 注释由此产生）+ `service/task.go`（4 处 `// AIFIRST:` — 三处 project_id stamp/继承 + 一处 advisory lock 复核）+ `service/project_presenter.go`、`handler/project_presenter.go`（新）+ `cmd/server/presenter_notification_listeners.go`（新）+ `cmd/server/router.go`（1 处 AIFIRST：presenter grant 端点）+ `handler/workspace_revoke.go`（1 处 AIFIRST：presenter grant 无 member FK，撤销成员时手工清理）+ `pkg/protocol/events.go`；前端 `presenter-control-sheet.tsx`（新）+ `issue-detail.tsx`/`inbox-page.tsx` 改 | presenter 单一写者控制权（D4）：**`ClaimAgentTask` 的串行化键从 `agent_id` 换成 `project_id`**（P0 §2.2），加 advisory lock 复核关竞态窗；presenter 非空时其他成员消息不被执行；Admin 在 Agent 空闲时可接管；6 个通知文案 + 权限面板 + chatHeader 主持人实时显示 | P2 三模式聊天交付切分 v2 CR-E。CR-2026-010 | 2026-08-02 | **本 fork 对上游语义改动最深的一行**。① `ClaimAgentTask` 谓词被改写，上游若动 claim SQL，必须重新论证 project 分支与「四 FK 全空」互斥类、chat_session 分支同时成立；② `service/task.go` 三处 stamp/继承（Create/Deferred/Retry）少贴一处就会让提升/重试的任务悄悄跌回旧的 per-(agent,issue) 序列化分支、绕过单写者保证——**不报错、只在并发下偶发**；③ `workspace_revoke.go` 的清理是因为 presenter_grant 没有指向 member 的 FK，上游改撤销流程时贴回；④ 三个迁移全撞号，`165` 必须保持 CONCURRENTLY 单语句独立文件。验证：`cd server && go test ./internal/service/ -run 'ClaimTaskCrossAgentProjectSingleWriter\|ClaimTaskChatSessionParallelWithProjectTask\|ClaimTaskConcurrentCapacityRespected' -v`（单写者语义的三把锁，含 `...SingleWriterStress`，**必须真库跑**，见 C6）+ `pnpm -C packages/views vitest run projects/components/presenter-control-sheet.test.tsx` |
| 23 | `server/pkg/protocol/events.go`（1 处 AIFIRST：`cr:updated` 投影更新事件）+ `core/types/events.ts`、`core/realtime/use-realtime-sync.ts`、`core/api/client.test.ts`；前端 `cr-gate-card{,.test}.tsx`、`cr-status-badge{,.test}.tsx`（新） | 门禁 UI 收尾：消息流内门禁状态条 + human_approval 审批卡（批准/驳回，reject_reason 注入 review_feedback）+ review 节点 blocker 列表与 reviewLoop attempt N/3 + chatHeader 的 CR 16 态徽标 | P2 三模式聊天 CR-F。CR-2026-011 TASK-05（#12–#15 的前端半边） | 2026-08-02 | `protocol/events.go` 的事件常量与前端 `core/types/events.ts` 必须成对贴回，上游新增事件类型时取并集。验证：`pnpm -C packages/views vitest run projects/components/cr-gate-card.test.tsx projects/components/cr-status-badge.test.tsx` |
| 24 | `server/internal/service/discussion_coordinator.go`（新）+ `handler/project_chat.go`（合并转发端点）+ `handler/comment.go`、`handler/daemon.go`、`handler/project.go`（DC agent 设置键）+ `cmd/server/router.go`（1 处 AIFIRST）+ `service/task.go`；前端 `discussion-pane.tsx` 多选合并预览 + `core/types/project.ts` | DC 讨论协调者 + 讨论转执行（D8）：DC 默认静默、@提及或回复激活；只协调不执行（execenv 只读 + forbidden 全部写 Skill）；可 `EnqueueTaskForMention` 路由到 Team Agent（显式标注 originator，满队走可审计的 queue-full 通知）；多选 Discussion 消息合并为带 `triggerMessage` + `chatHistory` 的 Team Agent 任务，无 CR 时可触发 requirement-register 升级为 CR | P2 三模式聊天交付切分 v2 CR-G。CR-2026-012 TASK-01~04、07~08 | 2026-08-03 | `handler/comment.go` 同时承载 #21 的触发豁免与本行的 DC 激活判定——上游改评论触发时**两条一起贴回**，只贴一条会让 Discussion 要么完全不响应 DC、要么每条消息都入队。验证：`cd server && go test ./internal/service/ -run DiscussionCoordinatorIDFromSettings -v` + `go test ./internal/handler/ -run 'DiscussionRoute\|DiscussionActivation\|MergeForwardDiscussion\|DiscussionContainerTaskIsAskOnly' -v` |
| 25 | `packages/views/chat/components/chat-input.tsx`{,`.test.tsx`}（上游组件重构）+ `views/editor/content-editor.tsx`、`extensions/index.ts`、`extensions/mention-suggestion.tsx` | ChatInput 解耦技术债（方案 A，纯前端）：抽出 `ChatInputCore` + 可选 `draftAdapter?: ChatInputDraftAdapter`（draftKey/editorKey/draft/attachments + 四个写方法）；**未传时落回默认实现读 `useChatStore`，`/chat` 页与浮窗零改动**；传入时完全按 adapter 渲染/回写、不触碰 `useChatStore`。回填 Team Agent 面与 Private Ask 面（附件槽 + @提及仅成员，接 `project-chat-store` 的 `{projectId}:{mode}` 命名空间） | P2 三模式聊天 CR-G 附带偿还。CR-2026-012 TASK-05/06 | 2026-08-03 | **前端冲突风险最高的一行**：改的是上游既有组件的内部结构。上游动 `chat-input.tsx` 时以上游为基底重新抽 Core + adapter，不要拿 fork 版整体覆盖。加法优先验证点 = 不传 `draftAdapter` 时行为与上游完全一致。验证：`pnpm -C packages/views vitest run chat/components/chat-input.test.tsx`（含「自定义 adapter 时不触碰 `useChatStore`」的双向隔离锁），外加手测 `/chat` 页与浮窗草稿仍正常 |

## 纯配置约定（无代码改动，部署时执行）

| # | 项 | 口径 | 原因 |
|---|---|---|---|
| C1 | `MULTICA_CLOUD_FLEET_URL` / `MULTICA_FLEET_URL` | 永远留空 | 空值 → CloudPATVerifier 为 nil → `mcn_` 云节点令牌天然 401（`server/internal/middleware/auth.go`），本地执行路线不用云节点 |
| C2 | `DISABLE_WORKSPACE_CREATION` | 先建组织 workspace，随后置 `true` 并重启后端 | 单组织内部部署；上游 `.env.example` 自带此两阶段引导（#3433） |
| C3 | `ALLOWED_EMAIL_DOMAINS` | 部署时填公司邮箱域 | 限制内网注册来源；与 `ALLOW_SIGNUP` 组合为 AND 语义 |
| C4 | `.env` 行尾必须全程 LF | Windows 上不要用会改写行尾的工具碰 `.env` | 实测踩坑（2026-07-30）：`.env.example` 为 CRLF，`make selfhost` 复制生成的 `.env` 带 `\r`，Postgres 卷用"密码+`\r`"初始化；之后任何把行尾规范成 LF 的编辑（如 Git Bash 的 `sed -i`）都会造成 backend 与已初始化卷的密码不一致（SASL auth failed）。修复方式：数据可弃时 `docker-compose down -v` 重建卷 |
| C5 | 本机 agent CLI 需已独立 `claude /login`（daemon 干净环境要求已根修，见代码改动 #2） | 实测踩坑（2026-07-31，CR-2026-001 TASK-04）：Claude Code 桌面 App 的登录态不共享给命令行 `claude`，须一次性 `/login` | 同左 |
| C6 | 本机跑 Go DB 集成测试：`DATABASE_URL` 必须显式取真密码（`docker exec multica-postgres-1 sh -c 'echo -n "$POSTGRES_PASSWORD"'`），走 5433 端口转发（`multica-pg-forward-5433` socat 容器） | 两个坑（2026-07-31，T07 期实测）：① postgres 容器密码是 `.env` 生成的 48 位随机串，不是 `multica/multica` 默认值——各 TestMain 连不上时**静默跳过且 `go test` 仍打 `ok`**，必须 `-v` 看到 `--- PASS` 才算测过；② 宿主机 5432 会被 WSL relay（`wslrelay.exe` 抢 `::1:5432`）干扰导致认证错乱，避开共享端口 | 防"绿灯没测"假阳性 |
| C7 | 项目级 settings 键（`project.settings` JSONB）：`team_agent_queue_limit`（默认 50）、`team_agent_id`、`discussion_coordinator_agent_id` | 由 owner/admin 经 `PATCH /api/projects/{id}` 配置，缺省/非法值回落代码侧默认 | CR-2026-004 / 006 / 012；`handler/project.go` 有白名单校验，新增键要同步白名单 |

## 已知测试失败基线（上游既有，非本 fork 引入）

双周 rebase 后跑全量测试时，以下失败为已知基线，不计入"本次改动引入的回归"；若数量或名单变化才需要排查：

| 测试 | 包 | 确认方式 | 记录日期 |
|---|---|---|---|
| `TestTraecliBlockedArgsFiltering` / `TestQoderBackendInvokesACPFlagAndFiltersBlockedArgs` / `TestQoderFiltersRemoteMcpWhenInitializeDoesNotAdvertiseCapability` | `server/pkg/agent` | `git stash` 摘除本地全部改动后仍失败（2026-07-31，Windows + 本机装有 Qoder 的环境）；根因未诊断，疑与测试对本机环境的隐含假设有关 | 2026-07-31 |
| `TestNewAPIClient_LeftoverMarkerActionableError` 等 7 项 | `server/cmd/multica` | 未改动的 main 检出 A/B 复跑结果完全一致（2026-07-31，CR-2026-002 TASK-05 全量基线时发现）；多为 Windows 路径分隔符/本机环境假设 | 2026-07-31 |
| `TestCLIConfig_BackwardCompat_*` 等 4 项 | `server/internal/cli` | 同上 A/B 验证一致 | 2026-07-31 |
| `TestEnsureDaemonID_Persists` / `TestListRuntimeLocalSkills_*` 等 26 项 | `server/internal/daemon` | 未改动 trunk 检出 A/B 失败名单逐条一致（2026-07-31，T10 全量时发现）；典型根因：测试改 `HOME` 但 Windows 代码走 `USERPROFILE`（daemon.id 写进真实家目录路径失败）、本机已装 IDE 的 skill 根目录被真实发现。上游 Windows 环境假设缺陷，非 fork 引入 | 2026-07-31 |
| `builtin_skills` 相关 / `TestShortTaskIDMatchesDaemon` / `TestParseSkillArchive_RejectsUnsafeSkillMdPath` | `server/internal/service` 等 | CR-2026-010 TASK-01 期复核：CRLF 检出与 Windows 路径分隔符断言差异，与 claim/队列逻辑无关 | 2026-08-02 |
| gofmt：本机对上游 794 个文件报格式差异 | 全仓 | **根因是 CRLF**：Windows autocrlf 检出的上游 .go 文件字节级 ≠ gofmt 输出（LF），并非格式真差异（T06 期核实，修正 T05 时"工具链版本差异"的初判）。**fork 新增 .go 文件以 LF 写入且必须过 gofmt**；上游文件不动 | 2026-07-31 |

## 未做（防止误以为已做）

- **Pipeline Runner**——`pipeline_run` / `pipeline_node_run` 两表已建（#12），但只有治理层的门禁节点投影器在写（#13，仅 4 个 human_approval 节点）；真正的 Runner（执行非 human 节点、驱动流水线）未做。
- **crctl `--caller` 到 Multica 用户的身份桥接**——因此 `canApprove`（#15）只能按工作区 owner/admin 判定，无法按 `cr.owners` 的角色判定（`canApprove` 注释内有 `ponytail:` 标注）。
- **controlled-shell L1 层（`--disallowedTools Bash`）**——agent 的 `permission.bash` 平台不持久化、无可信键，只落地了 L2 PATH shim + L3 IDE hooks（#8）。L2 用绝对路径可绕过，已在文档明示。
- **多知识库的 workspace→remote 映射表**——server 模式 reconcile（#10）当前假定单一知识库远端。
- **技能选择器在项目群聊输入区的排除**——CR-2026-012 §7 另议，未做。

> 已删除的旧「未做」条目（避免二次误导）：controlled-shell/gitguard 下沉 daemon 已由 #8 完成；CR 投影表与签名审批已由 #3/#7 完成。
