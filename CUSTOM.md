# CUSTOM.md — AI-First fork 二开台账

逐条记录本 fork 相对上游（`multica-ai/multica`）的所有定制。每次双周 rebase 前先核对本表（配合 `grep -rn "// AIFIRST:" server/ apps/`，见 [CONTRIBUTING.AIFIRST.md](CONTRIBUTING.AIFIRST.md) 规则二、三）。

## 代码改动

| # | 位置 | 改动 | 原因 / 追溯 | 日期 |
|---|---|---|---|---|
| 1 | `server/cmd/server/router.go`（2 处，均有 `// AIFIRST:` 标记） | 摘除 Stripe webhook（`/api/webhooks/stripe`）与 `/api/cloud-billing` 路由组的挂载；handler 代码保留未删 | 内网自托管无云端计费；访问即 404。CR-2026-001 FR-1（AI First Platform 仓库 `change-requests/CR-2026-001/`） | 2026-07-30 |
| 2 | `server/pkg/agent/claude.go#isFilteredChildEnvKey`（`// AIFIRST:` 标记） | 过滤名单补入 `CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST` 与 `CLAUDE_CODE_HOST_AUTH_ENV_VAR` 两个宿主托管认证标记 | 二分实证：两者任一单独泄漏给 daemon 拉起的 claude 子进程即报 `Not logged in`（凭据文件明明有效）；同批的 CHILD_SESSION/HOST_SESSION_ID/SDK_HAS_*_REFRESH 单独存在无害、未动。**候选回馈上游 PR**（上游注释明说该名单按名维护、欢迎补全）。CR-2026-001 TASK-04 | 2026-07-31 |
| 3 | `server/migrations/158_aifirst_cr_projection.{up,down}.sql`（新文件） | 新增治理投影三表：`cr` / `cr_sync_event` / `approval_record`（approve 用部分唯一索引 `WHERE decision='approve'`，reject 多条留痕） | P0 数据模型映射（git 权威 / PG 投影）落地；表可清空重放。rebase 时保持迁移编号顺延、勿与上游新迁移撞号。CR-2026-002 TASK-04 | 2026-07-31 |
| 4 | `server/internal/governance/`（新包：`actions.go`、`transitions_gen.go`、`gen/generate-transitions.mjs`、测试） | 治理层自研包：activity_log 两个 `aifirst.` action 常量 + CR 状态机只读副本（45 条展开转移，生成产物入库，`gen --check` 守一致性）+ 后续 crsync/approval/reconcile 落此包 | 规则一（自研代码住新目录）；构建不依赖 tools checkout（SDD-SUG-003）。状态机变更流程：改 tools dir-graph.yaml → 重跑 gen → 提交两仓。CR-2026-002 TASK-04 | 2026-07-31 |
| 5 | `server/internal/governance/crsync.go` + `server/cmd/server/router.go`（1 处 AIFIRST 挂载：`POST /api/daemon/cr-events`） | CR 投影 worker：事件幂等入账（`ON CONFLICT DO NOTHING`）→ per-CR 互斥 → 合法转移校验（transitions_gen）→ 更新 cr 行 / 乱序置 needs_reconcile → `cr:updated` 经 events.Bus 自动广播 workspace 房间。workspace 绑定只信 DaemonAuth 上下文，请求体 workspace_root_hash 仅日志用 | P1 §A 同步协议服务端半边；直接用 pgx 不走 sqlc（避免动上游 query 文件）。CR-2026-002 TASK-05 | 2026-07-31 |
| 6 | `server/internal/daemon/crevents.go`（新文件）+ `daemon.go`（1 处 AIFIRST 启动钩子）+ `config.go`（CRWorkspaceRoots 字段 + `MULTICA_CR_WORKSPACES` env）+ `client.go` 不动（ReportCREvents 定义在 crevents.go 里） | daemon CR 事件采集器：heartbeat 同周期扫 `{root}/.crctl/outbox/`（主）+ commit 兜底扫描（`.scan-cursor` 游标，四类 `[cr] ` 前缀契约）→ 双通道按幂等键合并（outbox 赢）→ 批 ≤100 上报 → 仅 accepted 删文件、rejected 三振进 `outbox/dead/`、上报失败游标不动（离线积压重试） | P1 §A.3。**部署口径**：daemon 启动环境需设 `MULTICA_CR_WORKSPACES=<knowledge-base 本地路径>`（分号分隔多工作区），未设则采集器完全关闭。commit 扫描的 git 调用暂直调 exec（只读 log/rev-parse），TASK-09 gitguard 收编（代码内 TODO）。CR-2026-002 TASK-06 | 2026-07-31 |
| 7 | `server/internal/governance/approval.go`（新）+ `crsync.go`（evidence 持久化）+ `crevents.go`（grants 轮询落盘 `.crctl/grants/`）+ `router.go`（2 处 AIFIRST 挂载：用户组 `/api/workspaces/{wid}/crs*` 三端点、daemon 组 `approvals/pending|ack`）+ 迁移 158 增列（cr_sync_event.evidence、approval_record.workspace_id/grant_json/delivered_at） | Ed25519 签名审批：`RequireHumanActor`（task_token 403）→ 证据漂移比对（409）→ 签发 grant（canonical 与 crctl 完全一致，共享向量锁定）→ approval_record 幂等落库（approve 部分唯一索引；重批返回首次 grant）→ daemon 队列 pending/ack 投递。私钥仅 `APPROVAL_SIGNING_KEY`（base64 PKCS#8）内存驻留，未配置=端点不挂载，配置无效=拒绝启动，日志只出 key_id | P1 §B。**部署口径**：`APPROVAL_SIGNING_KEY` + `APPROVAL_SIGNING_KEY_ID` 成对配置；公钥（`PublicKeyPEM()` 输出）提交进 knowledge-base 仓 `.crctl/keys/{key_id}.pub`。**跨工具接缝测试**：Go 签发 grant → 真实 crctl `approve --grant` 验签放行（`approval_crosscheck_test.go`，需 CRCTL_PATH）。CR-2026-002 TASK-08 | 2026-07-31 |
| 8 | `server/pkg/gitguard/`（新包）+ `cmd/multica/cmd_gitguard.go`（隐藏子命令 `gitguard-exec`）+ `internal/daemon/execenv/crguard_config.go`（新，PATH shim + Claude hooks 铸造）+ execenv `git.go`/`execenv.go`/`runtime_config_sections.go`（AIFIRST 挂钩：自守/shim 接线/上下文注入）+ `daemon.go`（PATH 前插 + CRCTL_WORKSPACE）+ `crevents.go`（TASK-06 TODO 收编，git 过闸） | controlled-shell 下沉：rules.json 的 Go 消费方（三元白名单 Check/Run）；每任务铸造 `{envRoot}/bin/git(.cmd)` shim 经 `multica gitguard-exec` 过闸；daemon 自身 worktree 操作 caller=system-orchestrator；Claude 后端自动物化 `.claude/settings.json` PreToolUse hooks；Agent 上下文注入 git 约束段 | P1 §C。**部署口径**：`MULTICA_CONTROLLED_SHELL_RULES=<rules.json 绝对路径>`，未设=不启用（上游行为不变），配置无效=fail-closed 拒绝一切 git。**层级**：L2 PATH shim（绝对路径可绕，文档明示）+ L3 IDE hooks（Claude）；**L1 --disallowedTools Bash 未接**（agent permission.bash 平台不持久化，无可信键；见任务完成记录）。**跨引擎修复**：rules.json 的 `add` 形态负向先行断言改 RE2 兼容（tools@4907539），Go RE2 不支持 `(?!)`。CR-2026-002 TASK-09 | 2026-07-31 |
| 9 | `server/internal/governance/audit.go`、`toolcalls.go`（新）+ `crsync.go`（audit 事件分流 3 处小改）+ `server/pkg/gitguard/spool.go`（新）+ `cmd/multica/cmd_gitguard.go`（拒绝路径 1 处接线）+ `internal/handler/daemon.go`（CompleteTask 2 处 AIFIRST 钩子） | AI 行为审计（零新增探针）：① gitguard-exec 拒绝 → `SpoolDenial` 写 outbox audit 事件（`{caller, sub, code}`，无参数正文）；② crctl gate/validate 漂移检出 → 同通道 audit 事件（摘要对，无证据内容）；服务端 `audit` 类事件绕过 cr_sync_event 账本直插 `activity_log`（action 白名单锁死两个 `aifirst.*` 常量，防伪造）；③ CompleteTask 从已持久化 task_message 流聚合工具调用摘要（工具名/目标路径/结果态，封顶 200 条）进 result JSONB | P1 §C.5/§B.4。**留证语义**：audit 无幂等键（无 commit sha），追加型，采集 ack 崩溃窗可能重复——审计计数可接受；同一漂移待采集期由确定性文件名去重。CR-2026-002 TASK-10 | 2026-07-31 |
| 10 | `server/internal/governance/reconcile.go`、`reconcile_github.go`（新）+ `crsync.go`（snapshot 分流）+ `cmd/server/main.go`（1 处 AIFIRST：sys_cron 任务注册）+ `internal/daemon/crevents.go`（快照上报 + `.crctl` 目录缺失时游标写失败的既有边角修复） | reconcile 对账双模式（git 权威、只读、不反向写 git）：server 模式 = sys_cron 任务（默认 5min）经 GitHub API 拉 origin HEAD + raw `_backlog.yml`；daemon 模式 = daemon 每 5min 把 {HEAD, raw backlog} 作为 `snapshot` 事件走既有 cr-events 通道。两模式汇聚 `ApplySnapshot`（单实现）：漂移覆写、needs_reconcile 清除、缺行补插、快照外行不动、未知状态永不投影 | P1 §A.4。**部署口径**：server 模式设 `REMOTE_RECONCILE_MODE=server` + `KNOWLEDGE_BASE_REMOTE_URL`（https://github.com/{owner}/{repo}）+ `KNOWLEDGE_BASE_TOKEN`（fine-grained 单仓 Contents:Read-only PAT，永不入日志）+ 可选 `RECONCILE_INTERVAL`；配置坏 = 拒绝启动。daemon 模式零配置（采集器开即带）。**新环境部署顺序**：先起 daemon 通道建立投影首行，再依赖 server 对账接管——server 模式不自举空投影（服务端不猜 workspace 绑定，设计约束）。多知识库需 workspace→remote 映射表（代码内 ponytail 注记）。CR-2026-002 TASK-07 | 2026-07-31 |

| 11 | `server/internal/governance/daemon_workspace.go`（新）+ `crsync.go`/`approval.go`（3 处绑定点改走 `resolveDaemonWorkspace`）+ 测试 | daemon workspace 绑定缺陷修复：原设计只认 `mdt_` daemon-token 绑定，但上游 `mdt_` 签发流程尚未接线（`auth.GenerateDaemonToken` 无调用方），真实 daemon 走 PAT 回退路径 → 治理三端点全 403。改为上游同款约定：`mdt_` 绑定优先；PAT 回退按服务端置的 `X-User-ID` 查 `member` 表绑定（单成员隐式、多成员需 `X-Workspace-ID`、非成员 403），请求体始终不可信 | CR-2026-002 TASK-11 实测暴露。此前 T05 集成测试用 `WithDaemonContext` 直接注入绕过了该路径，故单测全绿但真机 403。CR-2026-002 TASK-11 | 2026-07-31 |
| 12 | `server/migrations/161_aifirst_pipeline_runs.{up,down}.sql`（新文件）+ `server/pkg/db/queries/agent.sql`（`CreateRetryTask` 克隆列表补 `cr_id`/`pipeline_node_run_id`） | 新增 pipeline runner 状态投影两表：`pipeline_run` / `pipeline_node_run`（`detail` JSONB 是对 P0 §3.4 的单列增补，存 review 节点的 blocker 明细）；`agent_task_queue` 增 `cr_id`/`pipeline_node_run_id` 两列（B4）。两列由 StartTask 回调后置写入，`ClaimAgentTask` 序列化谓词与"四 FK 全空"互斥类不感知新列，无需改动 | P2 三模式聊天 CR-F（D7 门禁接合）；P0 §2.2/§3.4。首期写者是治理层门禁节点投影器（尚无 Runner），rebase 时保持迁移编号顺延、勿与上游新迁移撞号。CR-2026-011 TASK-01 | 2026-08-02 |
| 13 | `server/internal/governance/gate_nodes_gen.go`（新，生成产物）+ `gen/generate-gate-nodes.mjs`（新生成器）+ `gate_projection.go`（新）+ `crsync.go`（`applyStatus` 两处挂点：fresh-insert 分支与合法转移分支各接入 `projectGateTransition`） | 门禁节点投影器：从 crctl 状态事件流投影 `pipeline_run`/`pipeline_node_run`（仅 4 个 human_approval 门禁节点，尚无 Runner）。节点 UUID **不派生**——核实 tools pipeline-templates 的每个节点已带固定 id，生成器直接抽取（`gen --check` 守一致性，同 `transitions_gen.go` 模式），这就是 TSUG-002 要求的"单一决议点"。投影失败静默降级（DD-2：审批卡可见性只依赖 `cr.status`，不依赖本表）；`started_by` 无真实 actor，回退到 workspace 最早 owner | P2 三模式聊天 CR-F（D7 门禁接合）SDD DD-1/DD-6。CR-2026-011 TASK-02 | 2026-08-02 |
| 14 | `server/internal/daemon/crevents.go`（第 5 类 commit 正则 `crCommitReviewRe` + `buildReviewPayload`）+ `crsync.go`（`knownEventKinds` 增 `review` + `apply` 分派）+ `gate_projection.go`（新 `applyReview`） | review 事件通道：blocked/passed 评审对平台可见（此前无 advance 就无 outbox 事件，平台完全看不到 block）。`review-annotations/{stage}.yml` 读取方式是**工作树直读而非 `git show {sha}:path`**——已核实 controlled-shell `rules.json` 的 git 白名单不含 `show` 子命令，加它是 tools 仓的跨仓改动，超出本 CR 范围；`ponytail:` 标注了这一简化的天花板（daemon 扫描游标跨整轮 reviewLoop 才会失真）与升级路径。tools/crctl **零改动** | P2 三模式聊天 CR-F SDD DD-3。CR-2026-011 TASK-03 | 2026-08-02 |
| 15 | `server/internal/governance/project_gates.go`（新，`HandleProjectGates` + `canApprove` + `pendingApprovalStage`）+ `router.go`（`/api/projects/{id}/gates`，同一 `approvalSvc` 特性开关，挂在既有 project 路由组内不新写鉴权）+ `approval.go`（`HandleApprove` 增 `canApprove` 调用，403 `FORBIDDEN_APPROVER`）+ `pkg/db/queries/agent.sql`（`SetTaskCRAttributionIfValid`）+ `service/task.go`（`AttributeTaskToCR`）+ `handler/daemon.go`（`StartTask` 接受可选 `cr_id` 自报） | ① 项目群聊窗口读门禁数据的唯一入口：16 态 + 待批段 + 审批卡数据 + `pending_advance`（已批准待 crctl 推进的服务端派生态）+ 门禁节点历史；② `HandleApprove` 补角色策略——此前只查人类身份，任何工作区成员皆可批准，现收紧为 owner/admin（`cr.owners` 对应角色因无 crctl `--caller` 到 Multica 用户的身份桥接，`ponytail:` 标注暂缓，见 `canApprove` 注释）；③ `agent_task_queue.cr_id` 归因：daemon 从任务 workdir 分支名自报，服务端按调用方工作区校验后才落列，不信任自报内容 | P2 三模式聊天 CR-F SDD §4.3/§4.4/§4.5/DD-4/DD-5。**顺带修复**：`approval_test.go`/`resetCR` 缺 `approval_record` 清理，导致该测试在非临时（本地持久）数据库上二次运行必挂——预存的旧签名验不过新一次运行的公钥；已补清理，与本 CR 逻辑无关但在本任务测试中暴露。CR-2026-011 TASK-04 | 2026-08-02 |

## 纯配置约定（无代码改动，部署时执行）

| # | 项 | 口径 | 原因 |
|---|---|---|---|
| C1 | `MULTICA_CLOUD_FLEET_URL` / `MULTICA_FLEET_URL` | 永远留空 | 空值 → CloudPATVerifier 为 nil → `mcn_` 云节点令牌天然 401（`server/internal/middleware/auth.go`），本地执行路线不用云节点 |
| C2 | `DISABLE_WORKSPACE_CREATION` | 先建组织 workspace，随后置 `true` 并重启后端 | 单组织内部部署；上游 `.env.example` 自带此两阶段引导（#3433） |
| C3 | `ALLOWED_EMAIL_DOMAINS` | 部署时填公司邮箱域 | 限制内网注册来源；与 `ALLOW_SIGNUP` 组合为 AND 语义 |
| C4 | `.env` 行尾必须全程 LF | Windows 上不要用会改写行尾的工具碰 `.env` | 实测踩坑（2026-07-30）：`.env.example` 为 CRLF，`make selfhost` 复制生成的 `.env` 带 `\r`，Postgres 卷用"密码+`\r`"初始化；之后任何把行尾规范成 LF 的编辑（如 Git Bash 的 `sed -i`）都会造成 backend 与已初始化卷的密码不一致（SASL auth failed）。修复方式：数据可弃时 `docker-compose down -v` 重建卷 |
| C5 | 本机 agent CLI 需已独立 `claude /login`（daemon 干净环境要求已根修，见代码改动 #2） | 实测踩坑（2026-07-31，CR-2026-001 TASK-04）：Claude Code 桌面 App 的登录态不共享给命令行 `claude`，须一次性 `/login` | 同左 |
| C6 | 本机跑 Go DB 集成测试：`DATABASE_URL` 必须显式取真密码（`docker exec multica-postgres-1 sh -c 'echo -n "$POSTGRES_PASSWORD"'`），走 5433 端口转发（`multica-pg-forward-5433` socat 容器） | 两个坑（2026-07-31，T07 期实测）：① postgres 容器密码是 `.env` 生成的 48 位随机串，不是 `multica/multica` 默认值——各 TestMain 连不上时**静默跳过且 `go test` 仍打 `ok`**，必须 `-v` 看到 `--- PASS` 才算测过；② 宿主机 5432 会被 WSL relay（`wslrelay.exe` 抢 `::1:5432`）干扰导致认证错乱，避开共享端口 | 防"绿灯没测"假阳性 |

## 已知测试失败基线（上游既有，非本 fork 引入）

双周 rebase 后跑全量测试时，以下失败为已知基线，不计入"本次改动引入的回归"；若数量或名单变化才需要排查：

| 测试 | 包 | 确认方式 | 记录日期 |
|---|---|---|---|
| `TestTraecliBlockedArgsFiltering` / `TestQoderBackendInvokesACPFlagAndFiltersBlockedArgs` / `TestQoderFiltersRemoteMcpWhenInitializeDoesNotAdvertiseCapability` | `server/pkg/agent` | `git stash` 摘除本地全部改动后仍失败（2026-07-31，Windows + 本机装有 Qoder 的环境）；根因未诊断，疑与测试对本机环境的隐含假设有关 | 2026-07-31 |
| `TestNewAPIClient_LeftoverMarkerActionableError` 等 7 项 | `server/cmd/multica` | 未改动的 main 检出 A/B 复跑结果完全一致（2026-07-31，CR-2026-002 TASK-05 全量基线时发现）；多为 Windows 路径分隔符/本机环境假设 | 2026-07-31 |
| `TestCLIConfig_BackwardCompat_*` 等 4 项 | `server/internal/cli` | 同上 A/B 验证一致 | 2026-07-31 |
| `TestEnsureDaemonID_Persists` / `TestListRuntimeLocalSkills_*` 等 26 项 | `server/internal/daemon` | 未改动 trunk 检出 A/B 失败名单逐条一致（2026-07-31，T10 全量时发现）；典型根因：测试改 `HOME` 但 Windows 代码走 `USERPROFILE`（daemon.id 写进真实家目录路径失败）、本机已装 IDE 的 skill 根目录被真实发现。上游 Windows 环境假设缺陷，非 fork 引入 | 2026-07-31 |
| gofmt：本机对上游 794 个文件报格式差异 | 全仓 | **根因是 CRLF**：Windows autocrlf 检出的上游 .go 文件字节级 ≠ gofmt 输出（LF），并非格式真差异（T06 期核实，修正 T05 时"工具链版本差异"的初判）。**fork 新增 .go 文件以 LF 写入且必须过 gofmt**；上游文件不动 | 2026-07-31 |

## 未做（防止误以为已做）

- controlled-shell/gitguard 下沉 daemon（P1-F5，M0 明确范围外——当前 Agent 执行未受白名单约束）
- CR 投影表 / 签名审批 / Pipeline Runner 等 P1 schema 与服务（P0 映射表 §3，随 P1 的 CR 落地）
