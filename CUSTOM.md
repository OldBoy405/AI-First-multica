# CUSTOM.md — AI-First fork 二开台账

逐条记录本 fork 相对上游（`multica-ai/multica`）的所有定制。每次双周 rebase 前先核对本表，见 [CONTRIBUTING.AIFIRST.md](CONTRIBUTING.AIFIRST.md) 规则二、三。

**核对口径**（`// AIFIRST:` 单独一条不够用了——`.sql` 用 `-- AIFIRST:`，前端 `packages/` 一处标记都没有）：

```
grep -rnE "AIFIRST|CR-2026-[0-9]{3}" server/ packages/ --include=*.go --include=*.ts --include=*.tsx --include=*.sql --include=*.mjs | grep -v node_modules
```

**基线（2026-08-19 合并后）：172 个文件 / 503 处命中**，其中 `AIFIRST` 字样 106 处、`packages/` 下 2 处（chat-input.tsx 与 inbox-page.tsx 的 CR 溯源注释）。合并后这三个数只能升不能降；降了就是挂钩点被上游覆盖掉了。前端不带标记，靠《前端定制文件清单》一节逐个文件核对。

## 合并冲突总则

执行 `git merge upstream/main` 时：

- **两边都保留**——不做「全取上游」或「全取二开」，如果功能重叠，在上游基础上，将二开新增的功能补齐。
- **逻辑整合**——以上游为基底，把二开改动贴回正确位置；上游改名则跟着改名。
- **加法优先**——二开以新增为主；未使用新能力时行为应与上游一致。
- **标记挂钩**——上游文件中所有 `AIFIRST` / `CR-2026-xxx` 标注的挂钩点逐条保留，按上游新代码结构贴回正确位置（上游改名则跟着改名）；合并后用上面的 grep 与本台账逐条核对，确认无丢失、无失效。
- **生成产物不手工 resolve**——`transitions_gen.go` / `gate_nodes_gen.go` / `pkg/db/generated/*.go` 的冲突一律**丢弃冲突块、重跑生成器**（前两个 `node gen/generate-*.mjs`，后者 `make sqlc`）。真源是 `tools` 仓 dir-graph / pipeline-templates 与 `server/pkg/db/queries/*.sql`——`generated/*.go` 里的 `// AIFIRST:` 注释是 sqlc 从 `.sql` 里搬过去的，不是手改，改 `.sql` 才有效。
- **减法定制是总则例外**——本台账只有 #1 是「摘除上游能力」。对这一行「两边都保留 / 加法优先」不适用，正确动作是在上游新基底上**重新摘除**。其余行一律按总则走。
- **合并后必跑**——`pnpm typecheck` + `pnpm test`（前端/TS），`make test`（Go）；省事可直接 `make check`，另按《已知测试失败基线》一节排除上游既有失败。逐行的最小验证命令写在《代码改动》表「合并注意」列末尾的「验证：」。

## 迁移编号冲突（已发生，不是「注意别撞」）

fork 的 9 个迁移原占 158–166，上游 `upstream/main` **这 9 个号全部已被占用**（2026-08-19 合并时上游已排到 361；另 fork 的 265–268 与上游 issue_view 系列撞号）。合并后 fork 迁移已整体顺延为 **362–374**（158→362 … 166→370、265→371 … 268→374），文件已 `git mv` 改名，本机库 `schema_migrations` 已手工改号：

| 号 | fork | upstream/main |
|---|---|---|
| 158 | `158_aifirst_cr_projection` | `158_agent_task_queue_chat_input_task_id` |
| 159 | `159_project_settings` | `159_chat_message_message_kind` |
| 160 | `160_issue_origin_project_chat` | `160_chat_message_input_owner_index` |
| 161 | `161_chat_session_project` | `161_agent_skill_enabled` |
| 162 | `162_aifirst_pipeline_runs` | `162_resource_labels` |
| 163 | `163_issue_origin_project_discussion` | `163_agent_builder` |
| 164 | `164_agent_task_queue_project_id` | `164_attachment_task_id` |
| 165 | `165_atq_project_active_index` | `165_attachment_task_id_index` |
| 166 | `166_project_presenter_grant` | `166_project_dates` |

**合并口径**：fork 的 13 个整体顺延到上游末位之后（362 起，保持相对顺序不变）。注意三件事：

1. **`160` / `163` 必须保持相对先后**——两者都用 `DROP CONSTRAINT + ADD CONSTRAINT` 重写 `issue_origin_type_check` 的整张枚举表。上游若也动了这个 CHECK，重编号后要把**上游的枚举值并进来**再加 `project_chat` / `project_discussion`，否则会把上游新 origin_type 静默删掉。
2. **`165` 是 `CREATE INDEX CONCURRENTLY` 单语句迁移**，不能与别的语句合并进同一文件（迁移器限制，见上游 068）。
3. **已应用的库要手工修 `schema_migrations`**——重编号不是改个文件名就完事：已部署环境按旧号记过版本，直接改名会导致新号重跑（`IF NOT EXISTS` 大多幂等，但 158/162 建表不是）或旧号残留。重编号后对每个已存在的库核对一次版本表。本机库 2026-08-19 已执行（`schema_migrations.version` 旧号→新号，注意只改 fork 行：上游同名号迁移如 `162_resource_labels` 必须保留原号）。

4. **上游 214/215 已收编 `chat_session.project_id` 列**（软引用，无 FK；`214_chat_session_project` + `215_chat_session_project_index`，上游还建了同名 `idx_chat_session_project` 单列索引）。fork 的 161 顺延为 365 时**改写**：不再建列，只补 fork 语义依赖的三件套——FK（`chat_session_project_fk`，fork 保留硬引用）、复合查找索引（改名 `idx_chat_session_project_creator` 避让上游同名索引）、active 唯一索引（`chat_session_project_creator_active_unique`，get-or-create 并发收敛的锁）。

历史先例：CR-2026-010 的三个迁移在 worktree 里原编号 161–163，合并进 main 时因与 CR-008/009 已占用的号撞上而顺延为 164–166——顺延本身是走通过的流程，不是新发明。

## 前端定制文件清单（`packages/`，无 `AIFIRST` 标记，靠本清单核对）

**fork 新增（合并时不会冲突，但要确认没被上游同名文件顶掉）：**

- `packages/core/projects/project-chat-store{,.test}.ts`
- `packages/views/projects/components/`：`surface-tab{,.test}.ts`、`project-chat-panel{,.test}.tsx`、`project-team-agent-chat{,.test}.tsx`、`project-queue-bar{,.test}.tsx`、`project-queue-status{,.test}.tsx`、`project-private-ask{,.test}.tsx`、`discussion-pane{,.test}.tsx`、`presenter-control-sheet{,.test}.tsx`、`cr-gate-card{,.test}.tsx`、`cr-status-badge{,.test}.tsx`
- `packages/core/types/{trace,drift}.ts`、`packages/core/{trace,drift}/queries.ts`、`packages/core/api/drift-trace-schemas.test.ts`（CR-2026-049 新）
- `packages/views/governance/spec-trace/`、`packages/views/dashboard/drift/`（CR-2026-049 新）

**fork 改上游既有文件（真正的冲突源，按冲突风险排序）：**

| 文件 | 归属 CR | 改动性质 |
|---|---|---|
| `views/chat/components/chat-input.tsx`{,`.test.tsx`} | 012 | **风险最高**：把上游组件拆成 `ChatInputCore` + `ChatInputDraftAdapter`，默认 adapter 回落 `useChatStore`。上游改这个文件几乎必冲突，且必须保住「传自定义 adapter 时不触碰 `useChatStore`」这条单测锁 |
| `core/api/client.ts` | 004/006/007/008/009/010/011/012 | 新增端点方法，取并集 |
| `core/api/schemas.ts` | 006/007/009/010/011/012 | 新增 zod schema，取并集 |
| `core/projects/queries.ts` | 004/006/007/008/009/010/011 | 新增 query hooks，取并集 |
| `core/projects/mutations.ts`{,`.test.tsx`} | 006/007/010/011/012 | 新增 mutation hooks，取并集 |
| `views/editor/content-editor.tsx`、`extensions/index.ts`、`extensions/mention-suggestion.tsx` | 012 | @提及限成员：上游改编辑器扩展装配时贴回 |
| `views/projects/components/project-detail.tsx` | 004/006 | 主区 Issues\|Chat tab 壳（`?tab=` 深链）；上游改 project 详情布局时贴回 |
| `views/inbox/components/inbox-page.tsx`、`inbox-detail-label.tsx`、`inbox-page-routing.test.ts` | 009/010 | 容器 Issue 跳转条 + presenter 通知类型路由 |
| `views/modals/quick-create-issue.tsx`{,`.test.tsx`} | 004 | 429 `project_queue_full` 分支 |
| `views/issues/components/issue-detail.tsx` | 010 | presenter 转移作为审计条渲染 |
| `views/chat/components/chat-message-list.tsx` | 006 | 群聊复用消息流 |
| `core/realtime/use-realtime-sync.ts` | 004/011 | 新 WS 事件订阅 |
| `core/types/{issue,project,agent,inbox,events}.ts` | 004/006/009/010/011/012 | 新字段/新枚举值，取并集 |
| `views/locales/{en,ja,ko,zh-Hans}/{projects,inbox,modals}.json` | 004/006~012 | **四语言必须同步**，新增键取并集；漏一种语言 typecheck 不报、运行时掉 key |
| `core/api/client.ts`、`core/api/schemas.ts`、`core/types/index.ts`、`core/paths/paths.ts`、`core/diagnostics/diagnostic-context.ts`、`core/package.json` | 049 | trace/spec-search/drift 四组 zod + 五方法 + `governanceSpecDetail` 路径与 route pattern，取并集 |
| `views/search/search-command.tsx`{,`.test.tsx`}、`views/locales/*/search.json`、`views/dashboard/maturity/maturity-page.tsx`、`apps/web/app/[workspaceSlug]/(dashboard)/{drift,governance/specs/[specId]}/page.tsx`、`apps/desktop/src/renderer/src/routes.tsx` | 049 | Specs 搜索分组 + 漂移卡 + drift/spec-trace 页面与桌面路由，取并集 |

## 合并官方记录（摘要）

| 日期 | 对齐 | 冲突要点 |
|---|---|---|
| 2026-08-19 | `upstream/main` @ `b4137fc5b`（258 个提交，第二次同步） | 24 个冲突文件（2026-08-07 的 52 个减半）。核心：① 上游把 repo-checkout 认证从 body `auth_token` 改为 `Authorization: Bearer`（新 `registerActiveRepoCheckoutTask` 机制），fork 的 `activeTaskAuth` 任务身份校验保留并适配 header 回落（`health.go`）；`cmd_repo.go` 随上游改发 header。② 迁移全量顺延 362–374（见《迁移编号冲突》），`160/163` 的 CHECK 并上游 dingtalk/wecom 枚举；`161` 因上游 214/215 收编列而改写为纯约束补丁。③ `agent.sql` 冲突取并集：上游 `WHERE lock_task_owner_rows` 防 workspace-teardown 围栏与 fork 的 `project_id` 归因并存。④ 上游 API 变更适配 fork 代码：`DeleteComment` 返 `(DeleteCommentRow, error)`（新增 `commentFromCreateRow` 适配器）、`publishTaskEvent`/`publishChat` 增参、`DiscussionComposer.submitComment` 签名收窄、上游 `agent_task_queue_accountable_matches_originator(_strict)` CHECK 使 fork 测试 fixture 补 `accountable_user_id`、`hermesLaunchArgs` 被上游 hermesOverlay 机制吸收。⑤ 前端 `chat-input.test.tsx` 冲突重组：上游 focus describe 保留，fork adapter-isolation describe 完整贴回（含 spy 化 mock 与 `mockClear` 钩子）。⑥ `pnpm-lock.yaml` 由 pnpm 重解析（lockfile-only）。验证：Go 全量 build/vet 绿，governance/service/handler/daemon 定向套件全绿（含此前已知失败的 `TestTransitionTableShape`，断言已更新为 50）；TS 9 包 typecheck 全绿，chat-input 49 项、projects 九套件 118 项、core 135 项全绿。

| 2026-08-07 | `upstream/main` @ `c7a8b1699`（608 个提交，fork 首次同步上游） | 52 个冲突文件。① `chat-input.tsx`：上游重写为受控值 + 协同上传 composer（新增 ProjectPicker/`uploadEnabled`/`allowSubmitWhileRunning`/`draftKeyOverride`）；按总则以上游为基底，把二开 `ChatInputCore` + `ChatInputDraftAdapter`（CR-2026-012 DD-9/FR-8）追加保留在文件尾部，上游组件补回二开 `mentionItemTypes`、`onUploadFile`、`onRestoreDraftConsumed` 兼容 prop；废弃二开旧全局 wrapper `useGlobalChatDraftAdapter`（被上游 composer 取代）。② `agent.sql`/`chat.sql`：上游 attribution 列组与二开 `cr_id`/`pipeline_node_run_id`/`project_id` 取并集；生成物按总则丢弃冲突块重跑 `sqlc generate`。③ `service/task.go`（9 块）：上游 attribution 体系为基底，二开项目队列门禁/抑制抢占/Private Ask 投递贴回；`taskEvent` 上游改为方法，二开 `task_failure_event_test.go` 适配并强化 `ChatRecipientID` 断言。④ `router.go`：保留上游新增 VCS webhook 路由，Stripe 摘除（#1 减法定制）在新基底重新摘除。⑤ `computeCommentAgentTriggers` 上游改双返回值，二开 4 个测试文件 16 处调用点适配。⑥ daemon 层快照上报/`crevents.go` 挂钩按上游新事件结构贴回。验证：TS 五包 typecheck 全绿；core 1375 项、views chat/projects 定向套件全过；Go build/vet 绿，handler/service/governance/events/vcs/channel 等合并相关包全过，剩余失败见《已知测试失败基线》 |

## 已知测试失败基线

合并后检查中观察到、**非本次合并引入**的失败（下次合并时对照此表排除，新增失败才是回归）：

| 包 / 测试 | 性质 | 依据 |
|---|---|---|
| `internal/cli` 4 项（`TestCLIConfig_BackwardCompat_*`、`TestCLIConfig_OpenClawOverride_*`、`TestCLIConfig_ProfileCommandOverrides_*`） | 上游既有 | 已在 `upstream/main` 基线 worktree 复现同样失败（2026-08-07） |
| `internal/service` builtin-skills frontmatter 组（`TestBuiltinSkills*` 等 10 项） | 环境性 | 本机 `core.autocrlf=true` 检出 CRLF，测试断言 `"---\n"`；Linux/CI 上不会失败 |
| `internal/daemonws` 13 项、`internal/daemon`（`TestRefreshHealedVersion_*` 等）、`dingtalk`/`wecom`/`metrics`/`cmd/multica` 各 1–2 项 | 时序/网络敏感 | 全量并行跑失败，单独/整包复跑即过（daemonws 已复验）；daemon 包在本机会挂起，待上游基线单独复验归因 |
| `views/rich-content/rich-content-boundary.test.ts` 1 项（fence-language 例外排除） | 环境性 | 测试用正斜杠常量排除 `editor/extensions/code-block-view.tsx`，Windows 扫描路径为反斜杠导致误报；Linux/CI 不会失败（上游既有，非合并引入） |
| `views/autopilots/components/autopilot-dialog.validation.test.tsx` | 资源性 flake | 全量并行跑偶发失败，单独复跑稳定通过（2026-08-07 已复验） |

## 代码改动

治理链路（#1–#16）与 P2 三模式聊天（#17–#25）两条线。

| # | 位置 | 改动 | 原因 / 追溯 | 日期 | 合并注意 |
|---|---|---|---|---|---|
| 1 | `server/cmd/server/router.go`（2 处，均有 `// AIFIRST:` 标记） | 摘除 Stripe webhook（`/api/webhooks/stripe`）与 `/api/cloud-billing` 路由组的挂载；handler 代码保留未删 | 内网自托管无云端计费；访问即 404。CR-2026-001 FR-1（AI First Platform 仓库 `change-requests/CR-2026-001/`） | 2026-07-30 | **⚠ 总则例外：本行是减法定制**，不适用「两边都保留 / 加法优先」。上游改 Stripe／cloud-billing 挂载代码时以上游为基底**重新摘除**挂载（handler 保留）；路由组改名则跟改，两处 `// AIFIRST:` 标记保留。验证：起服务后 `curl -i .../api/webhooks/stripe` 与 `.../api/cloud-billing` 均须 404 |
| 2 | `server/pkg/agent/claude.go#isFilteredChildEnvKey`（`// AIFIRST:` 标记） | 过滤名单补入 `CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST` 与 `CLAUDE_CODE_HOST_AUTH_ENV_VAR` 两个宿主托管认证标记 | 二分实证：两者任一单独泄漏给 daemon 拉起的 claude 子进程即报 `Not logged in`（凭据文件明明有效）；同批的 CHILD_SESSION/HOST_SESSION_ID/SDK_HAS_*_REFRESH 单独存在无害、未动。**候选回馈上游 PR**（上游注释明说该名单按名维护、欢迎补全）。CR-2026-001 TASK-04 | 2026-07-31 | 上游在过滤名单新增条目时两边都保留；函数改名则跟改。若上游已收编这两条目，摘除挂钩并在台账标记已回馈上游。验证：`cd server && go test ./pkg/agent/ -run MergeEnvFiltersClaudeCodeVars -v` |
| 3 | `server/migrations/158_aifirst_cr_projection.{up,down}.sql`（新文件） | 新增治理投影三表：`cr` / `cr_sync_event` / `approval_record`（approve 用部分唯一索引 `WHERE decision='approve'`，reject 多条留痕） | P0 数据模型映射（git 权威 / PG 投影）落地；表可清空重放。CR-2026-002 TASK-04 | 2026-07-31 | **已与上游 158 撞号**，按《迁移编号冲突》一节整体顺延；本迁移是建表（非幂等），重编号后必须核对已应用库的 `schema_migrations`。验证：`make migrate-up` 后 `\d cr` 三表在 |
| 4 | `server/internal/governance/`（新包：`actions.go`、`transitions_gen.go`、`gen/generate-transitions.mjs`、测试） | 治理层自研包：activity_log 两个 `aifirst.` action 常量 + CR 状态机只读副本（**28 条声明 / 50 条展开**，生成产物入库，`gen --check` 守一致性）+ 后续 crsync/approval/reconcile 落此包 | 规则一（自研代码住新目录）；构建不依赖 tools checkout（SDD-SUG-003）。状态机变更流程：改 tools dir-graph.yaml → 重跑 gen → 提交两仓。CR-2026-002 TASK-04；CR-2026-031 TASK-06/TASK-12 补齐开发计划回退与 `code-approved -> developing` release-drift 回退，更新为 28/50 | 2026-07-31（数量 2026-08-12 更新） | 自研新包无冲突；`transitions_gen.go` 是生成产物，冲突丢弃后重跑生成器。关注上游对 activity_log 表结构／events.Bus 的改动，确保 `aifirst.*` 写入与 `cr:updated` 广播仍适配。验证：`node server/internal/governance/gen/generate-transitions.mjs --check` + `cd server && go test ./internal/governance/ -run 'TransitionTableShape\|IsLegalTransition\|ActionConstants' -v` |
| 5 | `server/internal/governance/crsync.go` + `server/cmd/server/router.go`（1 处 AIFIRST 挂载：`POST /api/daemon/cr-events`） | CR 投影 worker：事件幂等入账（`ON CONFLICT DO NOTHING`）→ per-CR 互斥 → 合法转移校验（transitions_gen）→ 更新 cr 行 / 乱序置 needs_reconcile → `cr:updated` 经 events.Bus 自动广播 workspace 房间。workspace 绑定只信 DaemonAuth 上下文，请求体 workspace_root_hash 仅日志用 | P1 §A 同步协议服务端半边；**治理包内**直接用 pgx 不走 sqlc（避免动上游 query 文件）——注意此约束只覆盖 governance 包，P2 聊天线（#17 起）确实改了 `queries/*.sql`。CR-2026-002 TASK-05 | 2026-07-31 | router.go daemon 组挂载点保留；上游重构路由则把该端点贴回新位置。events.Bus 广播随上游事件机制改名则跟改。验证：`cd server && go test ./internal/governance/ -run 'LegalFlowProjectsAndBroadcasts\|DualChannelSameEvent\|OutOfOrderFlagsReconcile\|IllegalTransitionFlagsReconcile' -v`（需真库，见 C6） |
| 6 | `server/internal/daemon/crevents.go`（新文件）+ `daemon.go`（1 处 AIFIRST 启动钩子）+ `config.go`（CRWorkspaceRoots 字段 + `MULTICA_CR_WORKSPACES` env）+ `client.go` 不动（ReportCREvents 定义在 crevents.go 里） | daemon CR 事件采集器：heartbeat 同周期扫 `{root}/.crctl/outbox/`（主）+ commit 兜底扫描（`.scan-cursor` 游标，四类 `[cr] ` 前缀契约）→ 双通道按幂等键合并（outbox 赢）→ 批 ≤100 上报 → 仅 accepted 删文件、rejected 三振进 `outbox/dead/`、上报失败游标不动（离线积压重试） | P1 §A.3。**部署口径**：daemon 启动环境需设 `MULTICA_CR_WORKSPACES=<knowledge-base 本地路径>`（分号分隔多工作区），未设则采集器完全关闭。commit 扫描的 git 调用暂直调 exec（只读 log/rev-parse），TASK-09 gitguard 收编（代码内 TODO）。CR-2026-002 TASK-06 | 2026-07-31 | daemon.go 启动钩子保留；上游改 daemon 初始化／heartbeat 结构则把钩子贴回新位置。上游给 heartbeat 加新逻辑时两边都保留。验证：`cd server && go test ./internal/daemon/ -run 'DualChannelMergedWithoutDuplicates\|PartialAcceptDeletesOnlyAckedFiles\|PoisonedFileQuarantined\|NetworkFailureKeepsBacklog\|ParseCRCommitMessage' -v` |
| 7 | `server/internal/governance/approval.go`（新）+ `crsync.go`（evidence 持久化）+ `crevents.go`（grants 轮询落盘 `.crctl/grants/`）+ `router.go`（2 处 AIFIRST 挂载：用户组 `/api/workspaces/{wid}/crs*` 三端点、daemon 组 `approvals/pending\|ack`）+ **迁移 158 内联增列**（cr_sync_event.evidence、approval_record.workspace_id/grant_json/delivered_at） | Ed25519 签名审批：`RequireHumanActor`（task_token 403）→ 证据漂移比对（409）→ 签发 grant（canonical 与 crctl 完全一致，共享向量锁定）→ approval_record 幂等落库（approve 部分唯一索引；重批返回首次 grant）→ daemon 队列 pending/ack 投递。私钥仅 `APPROVAL_SIGNING_KEY`（base64 PKCS#8）内存驻留，未配置=端点不挂载，配置无效=拒绝启动，日志只出 key_id | P1 §B。**部署口径**：`APPROVAL_SIGNING_KEY` + `APPROVAL_SIGNING_KEY_ID` 成对配置；公钥（`PublicKeyPEM()` 输出）提交进 knowledge-base 仓 `.crctl/keys/{key_id}.pub`。**跨工具接缝测试**：Go 签发 grant → 真实 crctl `approve --grant` 验签放行（`approval_crosscheck_test.go`，需 CRCTL_PATH）。CR-2026-002 TASK-08 | 2026-07-31 | router.go 两处挂载保留；上游改鉴权／路由组结构则在新基底贴回。**历史事实**：这批列是直接改进 158 号迁移文件的（当时 158 尚未在任何长期库上应用）；**今后一律新开顺延迁移，勿再手改 158**。验证：`cd server && go test ./internal/governance/ -run 'CanonicalDigestParity\|SigningKeyLoading\|Approve\|GrantDeliveryQueue\|ApprovalCardDoesNotLeak' -v`；跨工具接缝另跑 `CRCTL_PATH=<crctl 路径> go test ./internal/governance/ -run GrantCrossVerifiesWithCrctl -v` |
| 8 | `server/pkg/gitguard/`（新包）+ `cmd/multica/cmd_gitguard.go`（隐藏子命令 `gitguard-exec`）+ `internal/daemon/execenv/crguard_config.go`（新，PATH shim + Claude hooks 铸造）+ execenv `git.go`/`execenv.go`/`runtime_config_sections.go`（AIFIRST 挂钩：自守/shim 接线/上下文注入）+ `daemon.go`（PATH 前插 + CRCTL_WORKSPACE）+ `crevents.go`（TASK-06 TODO 收编，git 过闸） | controlled-shell 下沉：rules.json 的 Go 消费方（三元白名单 Check/Run）；每任务铸造 `{envRoot}/bin/git(.cmd)` shim 经 `multica gitguard-exec` 过闸；daemon 自身 worktree 操作 caller=system-orchestrator；Claude 后端自动物化 `.claude/settings.json` PreToolUse hooks；Agent 上下文注入 git 约束段 | P1 §C。**部署口径**：`MULTICA_CONTROLLED_SHELL_RULES=<rules.json 绝对路径>`，未设=不启用（上游行为不变），配置无效=fail-closed 拒绝一切 git。**层级**：L2 PATH shim（绝对路径可绕，文档明示）+ L3 IDE hooks（Claude）；**L1 --disallowedTools Bash 未接**（agent permission.bash 平台不持久化，无可信键；见任务完成记录）。**跨引擎修复**：rules.json 的 `add` 形态负向先行断言改 RE2 兼容（tools@4907539），Go RE2 不支持 `(?!)`。CR-2026-002 TASK-09 | 2026-07-31 | execenv 被上游重构时三处 AIFIRST 挂钩贴回新结构；daemon.go PATH 拼装跟上游。未配 `MULTICA_CONTROLLED_SHELL_RULES` 时行为必须与上游一致（加法优先验证点）。验证：`cd server && go test ./pkg/gitguard/ ./internal/daemon/execenv/ -v`（`TestCheckTable`/`TestRealRulesLoadUnderRE2`/`TestFromEnvSemantics`），并**不设** `MULTICA_CONTROLLED_SHELL_RULES` 再跑一次 execenv 测试确认与上游同行为 |
| 9 | `server/internal/governance/audit.go`、`toolcalls.go`（新）+ `crsync.go`（audit 事件分流 3 处小改）+ `server/pkg/gitguard/spool.go`（新）+ `cmd/multica/cmd_gitguard.go`（拒绝路径 1 处接线）+ `internal/handler/daemon.go`（CompleteTask 2 处 AIFIRST 钩子） | AI 行为审计（零新增探针）：① gitguard-exec 拒绝 → `SpoolDenial` 写 outbox audit 事件（`{caller, sub, code}`，无参数正文）；② crctl gate/validate 漂移检出 → 同通道 audit 事件（摘要对，无证据内容）；服务端 `audit` 类事件绕过 cr_sync_event 账本直插 `activity_log`（action 白名单锁死两个 `aifirst.*` 常量，防伪造）；③ CompleteTask 从已持久化 task_message 流聚合工具调用摘要（工具名/目标路径/结果态，封顶 200 条）进 result JSONB | P1 §C.5/§B.4。**留证语义**：audit 无幂等键（无 commit sha），追加型，采集 ack 崩溃窗可能重复——审计计数可接受；同一漂移待采集期由确定性文件名去重。CR-2026-002 TASK-10 | 2026-07-31 | handler/daemon.go CompleteTask 两处钩子保留；上游改 CompleteTask 流程则贴回新结构，摘要聚合基于上游的 task_message 持久化流。cmd_gitguard.go 拒绝接线跟子命令重构走。验证：`cd server && go test ./internal/governance/ -run 'Audit\|SummarizeToolCalls' -v` + `go test ./internal/handler/ -run AggregatesToolCallSummary -v` + `go test ./pkg/gitguard/ -run SpoolDenialWritesOutboxEvent -v` |
| 10 | `server/internal/governance/reconcile.go`、`reconcile_github.go`（新）+ `crsync.go`（snapshot 分流）+ `cmd/server/main.go`（1 处 AIFIRST：sys_cron 任务注册）+ `internal/daemon/crevents.go`（快照上报 + `.crctl` 目录缺失时游标写失败的既有边角修复） | reconcile 对账双模式（git 权威、只读、不反向写 git）：server 模式 = sys_cron 任务（默认 5min）经 GitHub API 拉 origin HEAD + raw `_backlog.yml`；daemon 模式 = daemon 每 5min 把 {HEAD, raw backlog} 作为 `snapshot` 事件走既有 cr-events 通道。两模式汇聚 `ApplySnapshot`（单实现）：漂移覆写、needs_reconcile 清除、缺行补插、快照外行不动、未知状态永不投影 | P1 §A.4。**部署口径**：server 模式设 `REMOTE_RECONCILE_MODE=server` + `KNOWLEDGE_BASE_REMOTE_URL`（https://github.com/{owner}/{repo}）+ `KNOWLEDGE_BASE_TOKEN`（fine-grained 单仓 Contents:Read-only PAT，永不入日志）+ 可选 `RECONCILE_INTERVAL`；配置坏 = 拒绝启动。daemon 模式零配置（采集器开即带）。**新环境部署顺序**：先起 daemon 通道建立投影首行，再依赖 server 对账接管——server 模式不自举空投影（服务端不猜 workspace 绑定，设计约束）。多知识库需 workspace→remote 映射表（代码内 ponytail 注记）。CR-2026-002 TASK-07 | 2026-07-31 | main.go sys_cron 注册为 AIFIRST 挂钩；上游改任务调度机制则贴回新位置。快照继续走 cr-events 通道，上游加新事件类别时两边都保留。验证：`cd server && go test ./internal/governance/ -run 'ParseBacklog\|ApplySnapshot\|SnapshotEventHeals' -v` |
| 11 | `server/internal/governance/daemon_workspace.go`（新）+ `crsync.go`/`approval.go`（3 处绑定点改走 `resolveDaemonWorkspace`）+ 测试 | daemon workspace 绑定缺陷修复：原设计只认 `mdt_` daemon-token 绑定，但上游 `mdt_` 签发流程尚未接线（`auth.GenerateDaemonToken` 无调用方），真实 daemon 走 PAT 回退路径 → 治理三端点全 403。改为上游同款约定：`mdt_` 绑定优先；PAT 回退按服务端置的 `X-User-ID` 查 `member` 表绑定（单成员隐式、多成员需 `X-Workspace-ID`、非成员 403），请求体始终不可信 | CR-2026-002 TASK-11 实测暴露。此前 T05 集成测试用 `WithDaemonContext` 直接注入绕过了该路径，故单测全绿但真机 403。CR-2026-002 TASK-11 | 2026-07-31 | 关注上游是否为 `auth.GenerateDaemonToken` 接线或改 daemon PAT 认证路径：若有，复核 `resolveDaemonWorkspace` 三处绑定点是否仍成立；新包本身无冲突。验证：`cd server && go test ./internal/governance/ -run 'PATFallbackBindsSingleMembership\|PATFallbackRejectsOutsiders' -v`，外加真机跑一次 daemon 确认治理三端点不 403 |
| 12 | `server/migrations/162_aifirst_pipeline_runs.{up,down}.sql`（新文件）+ `server/pkg/db/queries/agent.sql`（`CreateRetryTask` 克隆列表补 `cr_id`/`pipeline_node_run_id`） | 新增 pipeline runner 状态投影两表：`pipeline_run` / `pipeline_node_run`（`detail` JSONB 是对 P0 §3.4 的单列增补，存 review 节点的 blocker 明细）；`agent_task_queue` 增 `cr_id`/`pipeline_node_run_id` 两列（B4）。两列由 StartTask 回调后置写入，`ClaimAgentTask` 序列化谓词与"四 FK 全空"互斥类不感知新列，无需改动 | P2 三模式聊天 CR-F（D7 门禁接合）；P0 §2.2/§3.4。首期写者是治理层门禁节点投影器（尚无 Runner）。CR-2026-011 TASK-01。**编号更正**：在 worktree 里原为 161，合并进 main 时因与 CR-008 撞号已顺延为 162 | 2026-08-02 | **已与上游 162 撞号**，按《迁移编号冲突》一节整体顺延（建表非幂等，须核对 `schema_migrations`）。`agent_task_queue` 上游新增列时两边都保留，注意 Clone 列清单与 `ClaimAgentTask` 谓词对新列的感知。验证：`make sqlc` 后 `git diff --stat server/pkg/db/generated/` 只应含预期文件 |
| 13 | `server/internal/governance/gate_nodes_gen.go`（新，生成产物）+ `gen/generate-gate-nodes.mjs`（新生成器）+ `gate_projection.go`（新）+ `crsync.go`（`applyStatus` 两处挂点：fresh-insert 分支与合法转移分支各接入 `projectGateTransition`） | 门禁节点投影器：从 crctl 状态事件流投影 `pipeline_run`/`pipeline_node_run`（仅 4 个 human_approval 门禁节点，尚无 Runner）。节点 UUID **不派生**——核实 tools pipeline-templates 的每个节点已带固定 id，生成器直接抽取（`gen --check` 守一致性，同 `transitions_gen.go` 模式），这就是 TSUG-002 要求的"单一决议点"。投影失败静默降级（DD-2：审批卡可见性只依赖 `cr.status`，不依赖本表）；`started_by` 无真实 actor，回退到 workspace 最早 owner | P2 三模式聊天 CR-F（D7 门禁接合）SDD DD-1/DD-6。CR-2026-011 TASK-02 | 2026-08-02 | 上游改 `crsync.go` `applyStatus` 结构时，fresh-insert 与合法转移两分支重新贴回 `projectGateTransition` 挂点；`gate_nodes_gen.go` 是生成产物，冲突丢弃后重跑生成器。验证：`node server/internal/governance/gen/generate-gate-nodes.mjs --check` + `cd server && go test ./internal/governance/ -run GateProjection -v` |
| 14 | `server/internal/daemon/crevents.go`（第 5 类 commit 正则 `crCommitReviewRe` + `buildReviewPayload`）+ `crsync.go`（`knownEventKinds` 增 `review` + `apply` 分派）+ `gate_projection.go`（新 `applyReview`） | review 事件通道：blocked/passed 评审对平台可见（此前无 advance 就无 outbox 事件，平台完全看不到 block）。`review-annotations/{stage}.yml` 最初采用工作树直读；CR-2026-045 #34 已升级为受 controlled-shell 精确白名单约束的 `git show {sha}:path`，按历史 commit 读取，消除扫描游标跨 reviewLoop 时的轮次串线。 | P2 三模式聊天 CR-F SDD DD-3。CR-2026-011 TASK-03 | 2026-08-02 | 上游在 commit 扫描／事件分派处新增能力时两边都保留，`knownEventKinds` 补回 `review`；新正则与 `buildReviewPayload` 为新增，与上游能力并排贴回。验证：`cd server && go test ./internal/governance/ -run ApplyReview -v` + `go test ./internal/daemon/ -run ParseCRCommitMessageReviewContract -v` |
| 15 | `server/internal/governance/project_gates.go`（新，`HandleProjectGates` + `canApprove` + `pendingApprovalStage`）+ `router.go`（`/api/projects/{id}/gates`，同一 `approvalSvc` 特性开关，挂在既有 project 路由组内不新写鉴权）+ `approval.go`（`HandleApprove` 增 `canApprove` 调用，403 `FORBIDDEN_APPROVER`）+ `pkg/db/queries/agent.sql`（`SetTaskCRAttributionIfValid`）+ `service/task.go`（`AttributeTaskToCR`）+ `handler/daemon.go`（`StartTask` 接受可选 `cr_id` 自报） | ① 项目群聊窗口读门禁数据的唯一入口：16 态 + 待批段 + 审批卡数据 + `pending_advance`（已批准待 crctl 推进的服务端派生态）+ 门禁节点历史；② `HandleApprove` 补角色策略——此前只查人类身份，任何工作区成员皆可批准，现收紧为 owner/admin（`cr.owners` 对应角色因无 crctl `--caller` 到 Multica 用户的身份桥接，`ponytail:` 标注暂缓，见 `canApprove` 注释）；③ `agent_task_queue.cr_id` 归因：daemon 从任务 workdir 分支名自报，服务端按调用方工作区校验后才落列，不信任自报内容 | P2 三模式聊天 CR-F SDD §4.3/§4.4/§4.5/DD-4/DD-5。**顺带修复**：`approval_test.go`/`resetCR` 缺 `approval_record` 清理，导致该测试在非临时（本地持久）数据库上二次运行必挂——预存的旧签名验不过新一次运行的公钥；已补清理，与本 CR 逻辑无关但在本任务测试中暴露。CR-2026-011 TASK-04 | 2026-08-02 | 上游改 project 路由组时把 gates 端点贴回组内新位置；上游改 `HandleApprove` 时在其上叠加 `canApprove` 角色策略，勿覆盖；上游改 `StartTask` 时补回可选 `cr_id` 自报参数；`queries/agent.sql` 改后 `make sqlc` 重生成。验证：`cd server && go test ./internal/governance/ -run 'ProjectGates\|ApproveRejectsNonOwnerAdmin' -v` + `go test ./cmd/server/ -run SetTaskCRAttributionIfValid -v` |
| 16 | `server/internal/daemon/crevents.go`+测试、`governance/crsync.go`、`reconcile.go`、`reconcile_github.go` | 治理核心两处缺陷修补：① `projectableSha` 拦住占位符 sha 进投影指针——`--embedded` 模式下 `commit_sha` 为空串使幂等键 `(cr_id, commit_sha, event_kind)` 互撞，同一 CR 连续两次 embedded advance 的第二条 status 事件被 `ON CONFLICT DO NOTHING` 静默丢弃；② reconcile 只读 `_backlog.yml`，归档 CR 已移进 `_history.yml` 而永久失去自愈能力——新增 `ParseHistory`/`mergeAuthority`，server/daemon 双模式都把 `_history.yml` 并入权威快照，向后兼容 | CR-2026-002 归档收尾核对发现（CR-2026-001/002 当时卡在 `writing-back` + `needs_reconcile=true`，与真实 archived 不符）。CR-2026-003 TASK-01/02 | 2026-07-31 | 全部落在 fork 自有文件内，无上游冲突面。合并后确认 `_history.yml` 解析未被上游的 YAML 依赖升级破坏。验证：`cd server && go test ./internal/governance/ -run 'ParseHistory\|EmbeddedPlaceholderEventsDoNotCollide\|CheckpointFillsProjectedCommitForEmbedded\|SnapshotEventHeals' -v` |
| 17 | 迁移 `159_project_settings`（新）+ `server/internal/service/task_queue_capacity.go`（新）+ `service/task.go`（软上限语义）+ `handler/issue.go`（`writeProjectQueueFull` + 429 分支）+ `handler/project.go`（PATCH settings 白名单校验，`team_agent_queue_limit`）+ `handler/chat.go`、`handler/cancel_task_by_user_test.go`+ `pkg/db/queries/{agent,project}.sql` → `make sqlc` | Team Agent 共享队列容量治理（D1）：队列达上限（默认 50，per-project 可配）时非 owner/admin 入队拒 429 `project_queue_full`；owner/admin 不受限且 claim 时优先；用户可撤回自己的排队项（软删除留审计痕迹），槽位释放走既有 WS 通道广播 | P2 三模式聊天设计 §11 交付切分 D1。CR-2026-004 | 2026-08-01 | `159` 已与上游撞号（见《迁移编号冲突》）。`handler/issue.go` 的 429 分支挂在上游 quick-create 路径上——上游改这段错误处理时把 `writeProjectQueueFull` 分支贴回；`handler/project.go` 的 settings 白名单要与上游新增的 settings 键取并集（**别把上游新键挡掉**）。验证：`cd server && go test ./internal/handler/ -run 'ProjectQueueCapacity\|CancelTaskByUser' -v` + `pnpm -C packages/views vitest run modals/quick-create-issue.test.tsx` |
| 18 | 迁移 `160_issue_origin_project_chat`（新）+ `server/internal/service/project_chat.go`（新）+ `handler/project_chat.go`（新）+ `handler/issue.go`（三处列表/看板/搜索的 `origin_type IS DISTINCT FROM 'project_chat'` 隐藏过滤）+ `handler/project.go`、`pkg/db/queries/{issue,project}.sql` → `make sqlc`；前端见《前端定制文件清单》 | 三 tab 项目群聊窗口（D2 骨架 + D3 核心）：`project-detail` 主区新增 Issues\|Chat tab（`?tab=` 深链）、Chat 内三模式 tab 条、草稿按 `{projectId}:{mode}` 独立持久化；Team Agent 面走「隐藏容器 Issue + 薄发送端点」——容量守卫 → 落 comment → enqueue，满队同步 429 且评论不落库；消息渲染复用 issue-timeline/CommentCard/TimelineView | P2 三模式聊天交付切分 v2 CR-A。CR-2026-006 | 2026-08-01 | **`160` 撞号 + CHECK 约束整表重写**：`issue_origin_type_check` 是 DROP+ADD 全量枚举，上游若也动了这个 CHECK，必须把上游枚举值并进来再加 `project_chat`。`handler/issue.go` 的三处隐藏过滤是**容器 Issue 不外泄的唯一防线**，上游改列表/看板/搜索查询时逐处贴回，漏一处容器 Issue 就会出现在用户列表里。验证：`cd server && go test ./internal/handler/ -run 'ProjectTeamAgentID\|GetProjectDiscussion\|DiscussionContainerExcluded' -v` + `pnpm -C packages/views vitest run projects/components/project-chat-panel.test.tsx projects/components/project-team-agent-chat.test.tsx` |
| 19 | 后端仅 `handler/agent.go`（DD-1 发起人链路字段）+ `handler/cancel_task_by_user.go`（发起人永远可撤回自己任务的 fast-path 重排）+ `handler/chat.go`；其余为前端（`project-queue-bar`、`project-queue-status` 新增；`core/projects/{queries,mutations}.ts`、`core/types/agent.ts` 改） | D3 完整形态：队列条常驻「N 条排队」+ 展开逐项显示发起人 + 发起人撤回（403/409 结构化反馈）；停止（发送者停自己 / owner 停任意）；「只看 Agent 请求」本地过滤开关；消息悬浮仅复制。队列条经 WS 实时更新无需手动刷新 | P2 三模式聊天交付切分 v2 CR-B（纯前端为主，后端能力复用 CR-2026-004）。CR-2026-007 | 2026-08-02 | `cancel_task_by_user.go` 的改动是**权限判定顺序重排**（发起人 fast-path 先行），不是新增分支——上游改这段权限逻辑时不能简单取并集，要重新确认「发起人可撤回自己 + owner 可撤回任意」两条同时成立。验证：`cd server && go test ./internal/handler/ -run CancelTaskByUser -v`（`PrivateAgent_Originator_Succeeds` / `PrivateAgent_NonOriginatorMember_Returns403` / `OwnerCancelsAny` 三条必须同时绿）+ `pnpm -C packages/views vitest run projects/components/project-queue-bar.test.tsx` |
| 20 | 迁移 `161_chat_session_project`（新）+ `pkg/db/queries/chat.sql` → `make sqlc` + `handler/handler.go`、`handler/agent.go`、`handler/daemon.go`、`cmd/server/listeners.go`（per-user 推送）+ `internal/daemon/{daemon,health,types}.go`、`execenv/{execenv,runtime_config_sections}.go`（ask-only 强制）+ `cmd/multica/cmd_repo.go`；前端 `project-private-ask.tsx`（新） | Private Ask 面（D5）：`chat_session` 加 nullable `project_id`，按 `(project_id, creator_id)` 查会话，与既有全局 1:1 chat 并存；前端绕开 `use-chat-controller.ts`（耦合全局 `useChatStore` 单例），直接组合纯 props 的 ChatMessageList + ChatInput + TaskStatusPill；语义四差异：个人独立队列、**默认 Ask-only（execenv 不给写权限）**、仅本人可见、与 Team Agent 并行 | P2 三模式聊天交付切分 v2 CR-C。CR-2026-008 | 2026-08-02 | **隐私红线**：单 socket 架构下 Private Ask 消息不进全工作区广播，靠 `cmd/server/listeners.go` 的服务端 per-user 推送保证（前端过滤不算数）。上游改事件分发/监听器装配时必须重新验证这条，`listeners_scope_test.go` 是它的锁。ask-only 由 execenv 的 runtime config 段落实现，上游改 execenv 段落装配时贴回。`161` 撞号。验证：`cd server && go test ./cmd/server/ -run RegisterListeners -v` + `go test ./internal/handler/ -run 'GetProjectPrivateChat\|PrivateAskSessionExcludedFromGlobalChat' -v` + `go test ./internal/daemon/execenv/ -run 'ClassifyTask\|SlimKindMatrix' -v` |
| 21 | 迁移 `163_issue_origin_project_discussion`（新）+ `handler/issue.go`（隐藏过滤补 `project_discussion`）+ `handler/comment.go`（触发豁免：Discussion 评论不产生 agent 任务）+ `pkg/db/queries/{issue,project}.sql` → `make sqlc`；前端 `discussion-pane.tsx`（新）+ `inbox-page.tsx`/`inbox-detail-label.tsx`（容器跳转条） | Discussion 面（D6）：第二个隐藏容器 Issue（`origin_type='project_discussion'`，首次打开时 lazy 创建），纯人类多人聊天，复用 comment-card/reply-input/@提及/通知/订阅基建；输入区无模式/模型下拉、无上下文用量，只留 @ | P2 三模式聊天交付切分 v2 CR-D。CR-2026-009 | 2026-08-02 | 同 #18 的两条：**`163` 撞号 + CHECK 约束整表重写**（枚举现含 `project_chat` 与 `project_discussion` 两值，重编号后须保持在 160 之后）；`handler/issue.go` 隐藏过滤逐处贴回。**验收红线**：Discussion 消息不得产生任何 `agent_task_queue` 行（除非 #25 的 DC 被显式激活）——`handler/comment.go` 的触发豁免是唯一实现点，上游改评论触发逻辑时必须贴回。验证：`cd server && go test ./internal/handler/ -run 'DiscussionContainerExcluded\|ComputeCommentAgentTriggers_DiscussionContainer' -v` + `pnpm -C packages/views vitest run projects/components/discussion-pane.test.tsx` |
| 22 | 迁移 `164_agent_task_queue_project_id` / `165_atq_project_active_index`（CONCURRENTLY 单语句）/ `166_project_presenter_grant`（新）+ `pkg/db/queries/{agent,project_presenter}.sql` → `make sqlc`（`generated/agent.sql.go` 4 处 AIFIRST 注释由此产生）+ `service/task.go`（4 处 `// AIFIRST:` — 三处 project_id stamp/继承 + 一处 advisory lock 复核）+ `service/project_presenter.go`、`handler/project_presenter.go`（新）+ `cmd/server/presenter_notification_listeners.go`（新）+ `cmd/server/router.go`（1 处 AIFIRST：presenter grant 端点）+ `handler/workspace_revoke.go`（1 处 AIFIRST：presenter grant 无 member FK，撤销成员时手工清理）+ `pkg/protocol/events.go`；前端 `presenter-control-sheet.tsx`（新）+ `issue-detail.tsx`/`inbox-page.tsx` 改 | presenter 单一写者控制权（D4）：**`ClaimAgentTask` 的串行化键从 `agent_id` 换成 `project_id`**（P0 §2.2），加 advisory lock 复核关竞态窗；presenter 非空时其他成员消息不被执行；Admin 在 Agent 空闲时可接管；6 个通知文案 + 权限面板 + chatHeader 主持人实时显示 | P2 三模式聊天交付切分 v2 CR-E。CR-2026-010 | 2026-08-02 | **本 fork 对上游语义改动最深的一行**。① `ClaimAgentTask` 谓词被改写，上游若动 claim SQL，必须重新论证 project 分支与「四 FK 全空」互斥类、chat_session 分支同时成立；② `service/task.go` 三处 stamp/继承（Create/Deferred/Retry）少贴一处就会让提升/重试的任务悄悄跌回旧的 per-(agent,issue) 序列化分支、绕过单写者保证——**不报错、只在并发下偶发**；③ `workspace_revoke.go` 的清理是因为 presenter_grant 没有指向 member 的 FK，上游改撤销流程时贴回；④ 三个迁移全撞号，`165` 必须保持 CONCURRENTLY 单语句独立文件。验证：`cd server && go test ./internal/service/ -run 'ClaimTaskCrossAgentProjectSingleWriter\|ClaimTaskChatSessionParallelWithProjectTask\|ClaimTaskConcurrentCapacityRespected' -v`（单写者语义的三把锁，含 `...SingleWriterStress`，**必须真库跑**，见 C6）+ `pnpm -C packages/views vitest run projects/components/presenter-control-sheet.test.tsx` |
| 23 | `server/pkg/protocol/events.go`（1 处 AIFIRST：`cr:updated` 投影更新事件）+ `core/types/events.ts`、`core/realtime/use-realtime-sync.ts`、`core/api/client.test.ts`；前端 `cr-gate-card{,.test}.tsx`、`cr-status-badge{,.test}.tsx`（新） | 门禁 UI 收尾：消息流内门禁状态条 + human_approval 审批卡（批准/驳回，reject_reason 注入 review_feedback）+ review 节点 blocker 列表与 reviewLoop attempt N/3 + chatHeader 的 CR 16 态徽标 | P2 三模式聊天 CR-F。CR-2026-011 TASK-05（#12–#15 的前端半边） | 2026-08-02 | `protocol/events.go` 的事件常量与前端 `core/types/events.ts` 必须成对贴回，上游新增事件类型时取并集。验证：`pnpm -C packages/views vitest run projects/components/cr-gate-card.test.tsx projects/components/cr-status-badge.test.tsx` |
| 24 | `server/internal/service/discussion_coordinator.go`（新）+ `handler/project_chat.go`（合并转发端点）+ `handler/comment.go`、`handler/daemon.go`、`handler/project.go`（DC agent 设置键）+ `cmd/server/router.go`（1 处 AIFIRST）+ `service/task.go`；前端 `discussion-pane.tsx` 多选合并预览 + `core/types/project.ts` | DC 讨论协调者 + 讨论转执行（D8）：DC 默认静默、@提及或回复激活；只协调不执行（execenv 只读 + forbidden 全部写 Skill）；可 `EnqueueTaskForMention` 路由到 Team Agent（显式标注 originator，满队走可审计的 queue-full 通知）；多选 Discussion 消息合并为带 `triggerMessage` + `chatHistory` 的 Team Agent 任务，无 CR 时可触发 requirement-register 升级为 CR | P2 三模式聊天交付切分 v2 CR-G。CR-2026-012 TASK-01~04、07~08 | 2026-08-03 | `handler/comment.go` 同时承载 #21 的触发豁免与本行的 DC 激活判定——上游改评论触发时**两条一起贴回**，只贴一条会让 Discussion 要么完全不响应 DC、要么每条消息都入队。验证：`cd server && go test ./internal/service/ -run DiscussionCoordinatorIDFromSettings -v` + `go test ./internal/handler/ -run 'DiscussionRoute\|DiscussionActivation\|MergeForwardDiscussion\|DiscussionContainerTaskIsAskOnly' -v` |
| 25 | `packages/views/chat/components/chat-input.tsx`{,`.test.tsx`}（上游组件重构）+ `views/editor/content-editor.tsx`、`extensions/index.ts`、`extensions/mention-suggestion.tsx` | ChatInput 解耦技术债（方案 A，纯前端）：抽出 `ChatInputCore` + 可选 `draftAdapter?: ChatInputDraftAdapter`（draftKey/editorKey/draft/attachments + 四个写方法）；**未传时落回默认实现读 `useChatStore`，`/chat` 页与浮窗零改动**；传入时完全按 adapter 渲染/回写、不触碰 `useChatStore`。回填 Team Agent 面与 Private Ask 面（附件槽 + @提及仅成员，接 `project-chat-store` 的 `{projectId}:{mode}` 命名空间） | P2 三模式聊天 CR-G 附带偿还。CR-2026-012 TASK-05/06 | 2026-08-03 | **前端冲突风险最高的一行**：改的是上游既有组件的内部结构。上游动 `chat-input.tsx` 时以上游为基底重新抽 Core + adapter，不要拿 fork 版整体覆盖。加法优先验证点 = 不传 `draftAdapter` 时行为与上游完全一致。验证：`pnpm -C packages/views vitest run chat/components/chat-input.test.tsx`（含「自定义 adapter 时不触碰 `useChatStore`」的双向隔离锁），外加手测 `/chat` 页与浮窗草稿仍正常 |
| 26 | `server/internal/governance/approval_crosscheck_test.go`（test-only，沿用既有 `AIFIRST:` 注释） | 跨工具接缝向量扩展（CR-2026-030 TASK-07）：fixture 补最小 cr.md + v2 backlog（不再依赖旧 backlog status 回退布局），Tools Root 只从最终解析出的 crctl 路径派生；按 requirement / tech-design / dev-start / code 四 stage 表驱动 8 个 subtest，每个 stage 均验证 Go 签名 approve 首次消费 + 邻接 `changed=false`，以及签名 reject 经真实 crctl 权威回退 + 邻接 `changed=false` | 按 PRD FR-10.1 精确白名单：Multica diff 仅本测试文件 + CUSTOM.md，production code 与其它 test harness 零 diff；不新建集成框架。CR-2026-030 TASK-07 | 2026-08-11 | 全部落在 fork 自有测试文件内，无上游冲突面；英文注释、Go error 全检查。**数据库边界**：seam fixture 自身不读数据库，但包级既有 TestMain 在 DB 不可达时会整体 skip；最终无 skip 证据必须在可用 PostgreSQL 的环境取，不能为本 CR 越界修改 `crsync_test.go`。显式 `CRCTL_PATH` 时缺少 crctl 会直接失败。验证：`cd server && CRCTL_PATH=<tools worktree crctl.mjs> go test -count=1 -v ./internal/governance/ -run TestGrantCrossVerifiesWithCrctl` |
| 27 | `server/internal/governance/crsync_test.go`（test-only，沿用既有 `AIFIRST:` 注释）+ `CUSTOM.md` | archive 事件契约测试（CR-2026-032 TASK-04）：seed writing-back CR 投影 + 活动 feature-writeback `pipeline_run` → ingest schema v1 `archive` 事件（fixed real-looking SHA，与 crctl 确定性 dedup 文件名同构）→ 断言 `cr.status=archived`、`projected_commit`=事件 SHA、feature-writeback run `completed`、幂等键行数=1；重放同一事件断言账本不增行、投影/run 不变。不改 production 代码、迁移、query/generated 或 daemon schema | 验证 tools 侧 CR-2026-032 新增的 archive outbox 契约被既有生产消费者完整吸收（SDD §4.5/AC-07）；Multica production diff 必须为零，仅测试文件 + CUSTOM.md。CR-2026-032 TASK-04 | 2026-08-13 | 全部落在 fork 自有测试文件内，无上游冲突面；英文注释、Go error 全检查（CLAUDE.md）。**数据库边界同 #26**：包级 TestMain 在 DB 不可达时整体 skip（exit 0 假绿），无 skip 的 RUN/PASS 证据必须在可用 PostgreSQL 的环境取，不能为此修改 TestMain。验证：`cd server && go test -count=1 -v ./internal/governance/ -run TestArchiveEventCompletesWritebackRun`（须看到 `=== RUN` 与 `--- PASS`） |

| 28 | `ARCHITECTURE.md` (new root document) | Adds the stable repository map required by the `write-tech-design` contract: server/daemon/frontend boundaries, dependency direction, hard invariants, and negative space. No runtime behavior or executable contract is introduced. | CR-2026-045 architecture phase; created because the target repository had no root `ARCHITECTURE.md`. | 2026-08-17 | Keep the file as a concise map; update only for top-level boundary changes. Verification: review against `CLAUDE.md` and ensure no template placeholders remain. |
| 29 | `server/migrations/265_pipeline_run_architecture_active_unique.{up,down}.sql`、`266_agent_task_pipeline_node_active_unique.{up,down}.sql`（新文件，单语句 CONCURRENTLY） | Runner Core 并发唯一性：同一 `(workspace_id,pipeline_id,cr_id)` 最多一个非终态 architecture run；同一 `pipeline_node_run_id` 最多一个有效 task。DB unique violation 是竞态输家路径（重读，不重试） | CR-2026-045 TASK-04（SDD §3.3）。两个 partial unique index，不新增表/列/FK；`pipeline_node_run` 已有 `UNIQUE(run_id,node_id,attempt)` 直接复用 | 2026-08-17 | 迁移编号 265/266 按当前基线顺延；实施前若 freshness gate 发现上游已新增迁移，按《迁移编号冲突》一节重新确认唯一编号。down 文件保持单语句 `DROP INDEX CONCURRENTLY`。验证：真实 PostgreSQL 下 `go test ./internal/governance/ -run RunnerIndex -v`（四类并发断言） |
| 30 | `server/internal/governance/gen/generate-gate-nodes.mjs`（扩展）+ `gate_nodes_gen.go`（生成产物） | 生成器调用 tools `emit-registry.mjs --pipeline architecture-design`，把完整 registry JSON + canonical SHA-256 嵌入 `ArchitectureCoreRegistryJSON` / `ArchitectureCoreRegistryDigest`；旧 `0014` 节点 UUID 修正为当前 `0016`。build/runtime 不读 tools checkout | CR-2026-045 TASK-05（SDD §3.2）。单一 registry 生成器，不建第二个运行时模板 loader | 2026-08-17 | `gate_nodes_gen.go` 是生成产物，冲突丢弃后重跑生成器；`--check` 比较节点/prompt/permissions/replayLoop/digest。验证：`node server/internal/governance/gen/generate-gate-nodes.mjs --tools <tools-worktree> --check` |
| 31 | `server/internal/governance/runner.go`（新）+ `server/cmd/server/router.go`（2 处 AIFIRST：Runner 实例化/wiring + `POST /api/workspaces/{workspaceID}/pipeline-runs`） | Runner Core：固定 architecture-design 五节点纵切的单一幂等 `Reconcile(run)`。Start 只接受 task-token（`X-Actor-Source=task_token`）；CR 投影 requirement-approved + needs_reconcile=false；source task/executor agent/CR 同 workspace；digest 漂移 fail closed。唤醒：`cr:updated`/`task:completed`/`task:failed` 事件 + grant ACK（TASK-09 预留）+ 启动扫描。Runner 不解析 Agent 文本、不写 CR/Git、不签名 | CR-2026-045 TASK-07/TASK-09（SDD §3.4/§5/§6）。错误码 `RUNNER_*`/`TEMPLATE_DIGEST_MISMATCH` 结构化返回 | 2026-08-17 | 上游改 `router.go` 鉴权/路由组时把 Start 端点与 Runner wiring 贴回新位置；events.Bus 事件类型改名则跟改 `WireEvents`。验证：`go build ./cmd/server/` + `go test ./internal/governance/ -run Runner -v`（真库） |
| 32 | `server/pkg/db/queries/agent.sql`（`CreatePipelineTask`）+ `server/pkg/db/generated/agent.sql.go`（sqlc）+ `server/internal/service/task.go`（`EnqueuePipelineTask` + `PipelineTaskSpec` + `ErrRunnerAttributionInvalid`） | pipeline task 入队：从可信 source task 单 INSERT 复制完整 attribution snapshot（originator/accountable/source/delegation/rule/evidence），executor agent 同 workspace active + runtime 绑定、CR 同 workspace 投影存在，任一 guard 失败 INSERT 0 行 → `RUNNER_ATTRIBUTION_INVALID`。不重新分类归因，不把 logical owner 当用户 | CR-2026-045 TASK-06（SDD §3.5/B01）。`CreateRetryTask` 已原样继承 attribution/cr_id/pipeline_node_run_id，不新增 retry 分支 | 2026-08-17 | `agent.sql` 改后 `make sqlc` 重生成（本次生成物已提交）；上游新增 agent_task_queue 列时两边都保留。验证：`cd server && go test ./internal/service/ -run PipelineTaskAttribution -v`（真库）+ `go vet ./internal/service/` |
| 33 | `server/internal/daemon/types.go`（Task 增 Pipeline* 字段）+ `server/internal/daemon/prompt.go`（`buildPipelinePrompt` 分支）+ `server/internal/handler/agent.go`（AgentTaskResponse 增 Pipeline* 字段）+ `server/internal/handler/daemon.go`（`hydratePipelineContext` 挂点） | daemon pipeline task carrier：claim 时识别 `context.type=pipeline_node` 填充 wire 字段；`BuildPrompt` 对 pipeline task 直接返回固定 `PipelinePrompt`，不进入 issue/chat/quick-create prompt | CR-2026-045 TASK-08（SDD §3.5）。CR root/LocalWorkDir 解析由既有 `MULTICA_CR_WORKSPACES`/`workspace inspect` 承担，本 CR 只透传字段 | 2026-08-17 | 上游改 `Task`/`AgentTaskResponse` 结构时 Pipeline* 字段取并集；`buildPromptBody` 分派顺序保持 pipeline 分支在最前。验证：`go build ./internal/daemon/ ./internal/handler/` |
| 34 | `server/internal/daemon/crevents.go`（`buildReviewPayload` + `stageAnnotationFile` + `normalizeBlockers`）+ `crevents_test.go` | review 事件 parity：commit-scan fallback 用显式 stage→文件映射（`tech-design→sdd.yml`），通过受限 `git show {sha}:path` 读取该轮历史 annotation，scalar/结构化 blocker 归一化为 `[]string`，payload 补 `subject_sha256`，与 crctl outbox 同字段集合；旧 payload 缺 Core 字段时 Runner fail closed | CR-2026-045 TASK-10（SDD B05）。tools controlled-shell 仅放行 40 位 commit SHA + canonical review annotation 路径，outbox 优先仍保留 | 2026-08-17 | 上游改 `buildReviewPayload`/`reviewAnnotationDoc` 时保留 exact-commit 读取、stage 映射与 blocker 归一化；`knownEventKinds` 补回 `review`。验证：`cd server && go test ./internal/daemon/ -run 'ParseCRCommitMessageReview\|BuildReviewPayload' -v` |

| 35 | `server/internal/governance/runner.go` + `server/internal/governance/runner_integration_test.go` | Runner Core recoverability: template digest drift is non-terminal and resumes same run; failed/cancelled `push-progress` retries only the checkpoint node with one active successor; real PostgreSQL coverage locks both semantics | CR-2026-045 CODE-B08/B09 | 2026-08-18 | 上游改 Runner reconcile/partial unique indexes时保留 same-run digest recovery 与 checkpoint-only retry；验证：`DATABASE_URL=... go test ./internal/governance -count=1`（真实 PostgreSQL） |
| 38 | 第二次上游合并的 fork 侧适配（无单一 CR 归属）：`internal/daemon/health.go`（activeTaskAuth 校验增加 `Authorization` header 回落）、`health_test.go`（fork 测试适配 bearer 注册机制 + Windows 路径 `filepath.ToSlash`）、`cmd/multica/cmd_repo.go`（body `auth_token` 字段随上游移除）、`internal/service/{discussion_coordinator,project_chat}.go`（`commentFromCreateRow` 适配 sqlc 新返回行）、`task.go`/`mika_onboarding.go`（`publishTaskEvent`/`publishChat` 增参）、`cancel_task_by_user_test.go`/`project_queue_capacity_test.go`（fixture 补 `accountable_user_id` 满足上游 strict CHECK）、迁移 362–374 顺延与 `364/367` CHECK 并枚举、前端 `chat-input.test.tsx`（adapter-isolation describe 重组 + spy mock）、`discussion-pane.tsx`（submitComment 签名适配） | 上游 258 提交合并（`b4137fc5b`）。合并总则适配：上游改签名则跟改、上游收编则改写（161→365）、上游新约束则 fixture 补列 | 2026-08-19 | 下次合并前对照本行复核：上游再改 checkout 认证协议时重贴 `health.go` 回落；上游再改 `agent_task_queue` 归因约束时复核 fork fixture；`commentFromCreateRow` 随 sqlc 行结构同步。验证：`go test ./internal/daemon/ -run 'RepoCheckout'`、`go test ./internal/handler/ -run 'CancelTaskByUser|ProjectQueueCapacity'`、`pnpm -C packages/views vitest run chat/components/chat-input.test.tsx` |
| 36 | `server/internal/daemon/pipeline_task.go`、`daemon.go`、`execenv/{context,execenv}.go` + tests | Pipeline task controlled environment: task-local `crctl` launcher, validated `CRCTL_OPERATIONAL_WORKSPACE`/`GIT_CONFIG_GLOBAL` trust config, `.crctl` writable root for Codex, and no generic worktree sidecars that would make workspace preflight dirty | CR-2026-045 CODE-B10 | 2026-08-18 | 上游改 execenv Prepare 或 Codex shell policy 时保留 pipeline sidecar opt-out与路径 containment；验证：`go test ./internal/daemon/... -run 'Pipeline|ConfigurePipelineGit|InstallPipeline' -count=1` |
| 37 | `server/internal/governance/reconcile.go` + `reconcile_test.go`、`gate_nodes_gen.go`（生成更新）、`server/migrations/267_issue_origin_type_restore.*.sql`、`268_issue_origin_type_restore_validate.*.sql`、`server/internal/service/project_chat_test.go` | E2E hardening：active architecture pipeline 期间 root snapshot 不覆盖 live CR projection；architecture push-progress registry 去除未解析 workspace placeholder；向前 repair migration 恢复 `project_chat`/`project_discussion` + dingtalk/wecom 的完整九值 issue origin constraint | CR-2026-045 TASK-13/TASK-14/TASK-15（SDD §3.7） | 2026-08-18 | 上游改 snapshot reconcile 时保留 active-run guard；generated registry 冲突时从 CR tools HEAD 重生；未来任何 `issue_origin_*` migration 重建 CHECK 必须携完整九值集合。验证：`go test ./internal/governance -run TestApplySnapshotSkipsActiveArchitecturePipeline -count=1 -v`、`go test ./internal/service -run 'Test(EnsureProjectChatAndDiscussionIssue_ShareTheSamePlumbing|ProjectContainerOriginConstraintRejectsUnknownOrigin)' -count=1 -v`、`go run ./cmd/migrate up` |


| 39 | `server/migrations/375_maturity_snapshot_table`..`379_maturity_report_history`（10 文件）+ `server/cmd/migrate/main.go`（concurrentIndexCleanups 补 369/371/372/376/378/379 六项） | 新增 CR-A 唯一新表 `maturity_snapshot`（tenant key 含 workspace_id，375 无内联索引 → 376 UNIQUE CONCURRENTLY → 377 PK USING INDEX → 378 scope/date 查询索引）+ 379 completed report history partial index（区别于 369 的 active-task 索引）；顺手补齐上游遗留 369/371/372 的 cleanup registry 项（TestEveryConcurrentUpBuildHasCleanup 归零） | CR-2026-047 TASK-02。真实 PG up/down/up 已执行（379→375 逆序全部回滚成功，`down` 在 373 因上游存量 CHECK 冲突中止与本 CR 无关）；EXPLAIN 断言 378/379 命中见 `maturity_index_explain_test.go` | 2026-08-20 | 上游新增 375–379 迁移号时按《迁移编号冲突》一节顺延改名；`concurrentIndexCleanups` 六项与 down 无冲突。验证：`cd server && go test ./cmd/migrate/ -run 'Concurrent' -v` + `go test ./internal/service/ -run TestMaturityIndexesServeTheirQueries -v` |
| 40 | `server/internal/maturity/`（新包：schema.go、score.go、api.go、config_gen.go、gen/generate-config.mjs + test） | 成熟度领域纯类型 + 计分纯函数（不可用指标拒绝部分权重重归一化）+ 读 API 响应类型 + knowledge-base `maturity-config.yaml` 的零依赖生成器（CRLF 规范化、hard fail、dirty guard、`--check`） | CR-2026-047 TASK-01/TASK-03/TASK-08。observing seed 固定 8×0.125/0/1，CR-D 人审前不改 | 2026-08-20 | 自研新包无上游冲突；`config_gen.go` 是生成产物，冲突丢弃重跑 `node server/internal/maturity/gen/generate-config.mjs --source <kb-worktree>`。验证：`node --test server/internal/maturity/gen/generate-config.test.mjs` + `cd server && go test ./internal/maturity/ -v` |
| 41 | `server/pkg/db/queries/maturity.sql`（新建，五节）+ `server/pkg/db/generated/maturity.sql.go`（sqlc） | CR-A 全部 30 个 Maturity* 查询：Token/Agent 指标、CR/Pipeline 指标、治理护栏（含 `cr.owners` free-text 只检测不匹配名称/不强转 UUID）、基线 P10/P75（percentile_cont 在 PG 内）、rollup 写路径、读路径、Org Admin 查找、retry-eligible plans、同周 inbox 串行去重；周报按 immutable `report_key` 排序并用 keyset cursor 分页，不受新完成任务插入影响 | CR-2026-047 TASK-04/05/06/07/08/10；code-review attempt 1 后执行 pinned `make sqlc` 完整重生，`MaturitySnapshot` 由 schema 自动生成，不再手补 generated 文件；model trend SQL 按 Asia/Shanghai 日分桶，latest org/报告分别按 bucket/report key DESC | 2026-08-20 | 任何 query/schema 修改一律 `make sqlc`，提交生成器真实输出，禁止手补 generated。上游改 task_usage/cr/pipeline 表时核对 join 键。验证：`cd server && go build ./...` |
| 42 | `server/internal/service/maturity_rollup.go`+`maturity.go`+`maturity_report.go`+`org_admin.go` + 对应测试 | rollup 事务编排（advisory lock/精确目标桶 no-op/旧桶补洞/8 指标+治理/owner-unresolved 传播/scores={}；user scope 合并当日 usage initiator，覆盖旧 task 当日产 Token）+ `maturity_metrics.go` 指标 helper（rollup 主文件 <800 行）+ 六端点读服务（latest/daily trend/28 日 baseline；未知 metric/坏 cursor 400，持久化 JSON 损坏 fail-closed）+ 周报 envelope 完成事务（SHA/task/chat/path/五节校验 + Owner inbox）+ Org Admin 幂等初始化（org-admin/项目 lead/rule version/Autopilot） | CR-2026-047 TASK-06/TASK-08/TASK-10。service 不能 import governance（runner 反向依赖），三个 review node UUID 以包内常量 + 外部测试交叉核对（`maturity_rollup_crosscheck_test.go`） | 2026-08-20 | 上游改 governance.ReviewGateNodes 的 node UUID 时交叉核对测试会红，同步更新常量；上游改 autopilot/agent 建表参数时适配 EnsureOrgAdminWorkspace。验证：`cd server && go test ./internal/service/ -run 'Maturity|TestBuildReportEnvelope|TestEnsureOrgAdminWorkspaceIdempotent|TestPreviousLocalDate|TestDayWindow|TestPercentile|TestCostFrom|TestTaskCoverage|TestNormalize|TestValidateSnapshot' -v` |
| 43 | `server/internal/scheduler/jobs_maturity.go` + test + `server/cmd/server/main.go`（1 处 AIFIRST 注册） | 每日 00:30 Asia/Shanghai 快照 job：hook 全权规划（retry-eligible FAILED 合并 + 7 日 oldest-first + 首启仅最近 plan），CatchUpMode/Window 仅声明意图 | CR-2026-047 TASK-07。handler 每 plan 只写前一自然日，不在 handler 二次扩窗 | 2026-08-20 | 上游改 JobSpec 字段或 main.go 注册区时把 `schedulerMgr.Register(scheduler.MaturitySnapshotJob(pool))` 贴回新位置。验证：`cd server && go test ./internal/scheduler/ -run TestMaturity -v` |
| 44 | `server/internal/handler/maturity.go`+`org_admin.go`+`maturity_test.go` + `server/cmd/server/router.go`（`/api/maturity/*` 六 GET + `/api/agents/org-admin` POST） | 看板读端点（user ranking 400、非 self 趋势 400、日期/limit 校验、ApiError 三件套）+ Org Admin bootstrap 端点（Owner/Admin、runtime 同 workspace 校验） | CR-2026-047 TASK-08/TASK-10 | 2026-08-20 | 上游改 router 受保护组结构时把两处挂载贴回 Auth 组。验证：`cd server && go test ./internal/handler/ -run TestMaturityHandlerGuards -v` |
| 45 | `server/internal/service/builtin_skills/multica-maturity-weekly-report/SKILL.md`（新内置 skill，go:embed 自动打包） | 周报五节模板 + 数据来源（maturity API 的 baseline_suggestions）+ 显式 Write/mkdir/sha256sum/mv 权限完成同目录原子 temp/rename 写盘 + 只输出 direct envelope（schema/task/chat/SHA/config revisions），由 server 完成事务校验并通知 Owner | CR-2026-047 TASK-10 | 2026-08-20 | 上游改 builtin_skills embed 目录时随目录迁移。验证：`cd server && go test ./internal/service/ -run BuiltinSkills -v` |
| 46 | `packages/core/`（`types/maturity.ts`、`types/org-admin.ts`、`maturity/queries.ts`、`api/schemas.ts` zod 六组、`api/client.ts` 七方法、`api/maturity-schemas.test.ts`、`paths/*` maturity 段、`package.json` 导出）+ `packages/views/dashboard/{components/{dim-segmented,usage-trend-card,leaderboard},maturity/}`（页面/建议面板/定义页/测试）+ `packages/views/layout/`（侧边栏 maturity 项 + BrainCircuit 图标）+ `packages/views/search/`（搜索别名）+ `packages/views/locales/*/layout.json` + `apps/web` maturity 页面 + `apps/desktop` 路由 | 前端看板全链路：API boundary 将 snake_case wire JSON 转为 camelCase domain model；原生 date range + Owner mode、真实复用三件式共享组件（观察期无雷达）、project/self/model 日/周趋势与 config revision 断点、Token/门禁成对、8 项可刷性、治理 unavailable、项目排名、建议历史/追问深链、Owner/Admin runtime 选择并初始化 Org Admin、语义 typography token/AppLink、反 Goodhart 页脚 | CR-2026-047 TASK-08/TASK-09/TASK-10 | 2026-08-20 | 前端定制文件清单：`packages/core/{types/maturity.ts,types/org-admin.ts,maturity/*,api/schemas.ts,api/client.ts,paths/*,diagnostics/diagnostic-context.ts,package.json}`、`packages/views/{dashboard/maturity/*,layout/app-sidebar.tsx,layout/route-icon-components.tsx,search/search-command.tsx,locales/*/layout.json}`、`apps/web/app/[workspaceSlug]/(dashboard)/maturity/page.tsx`、`apps/desktop/src/renderer/src/routes.tsx`。验证：`pnpm -C packages/core typecheck` + `pnpm -C packages/views typecheck` + `pnpm -C packages/core vitest run api/maturity-schemas.test.ts paths` + `pnpm -C packages/views vitest run dashboard/maturity layout search` |

| 47 | `server/internal/service/autopilot.go`、`task.go`、`maturity_completion.go`（新）+ `server/internal/daemon/prompt.go` + `server/pkg/db/queries/autopilot.sql`（及 sqlc 生成物） | E3 生产接缝：canonical 周报 run_only dispatch 前要求 Org Admin 项目已有有效 `local_directory`，缺失则不建 task、run 记 skipped 并给 Owner 写可操作 inbox；通过后为 task 写 `project_id`，按 project+Owner 复用 Team Agent `chat_session`；prompt 暴露 task/chat ID；terminal completion 以持久化 task→autopilot_run→canonical Org Admin Autopilot identity（不信任 payload）识别 schedule report，在同一事务把 daemon wrapper 规范化为 direct report envelope、校验 schema/key/path/SHA/五节/task/chat，并用事务级 advisory lock 保证跨 task 的同周 Owner inbox 唯一；普通 Autopilot 与 Org Admin 后续追问路径不变 | CR-2026-047 code-review attempt 1 修复 AC-18/19/21/22；`// AIFIRST:` 挂点在 autopilot/task/prompt 与 query | 2026-08-20 | 上游改 `dispatchRunOnly`、`CompleteTask`、Autopilot task SQL 或 prompt 时保留四处挂点；migration 379 依赖 direct `result->>'schema'`，不得退回只存 TaskCompletedPayload wrapper。验证：`cd server && go test ./internal/service -run 'TestEnsureOrgAdminWorkspaceIdempotent|TestMaturityReportBuiltinSkillContract|TestMaturityConfigExposesTwentyEightDayBaseline' -count=1 -v` |

| 48 | `server/migrations/385~397_*`（26 文件）+ `server/cmd/migrate/main.go`（concurrentIndexCleanups 补 386/388/389/391/392/393/396，concurrentDownIndexCleanups 补 395/397）+ `server/internal/governance/{crsync,approval,reconcile,runner}.go` + `server/pkg/db/queries/maturity.sql`（sqlc 生成物）+ `server/internal/service/maturity_rollup_db_test.go` + `server/cmd/migrate/migrate_workspace_seam_test.go` + `server/internal/governance/workspace_contract_test.go` | E4/E5 租户隔离迁移：`drift_finding` 表（385–389：无内联 PK/FK、USING INDEX、dedup/keyset CONCURRENTLY）；`cr_sync_event.workspace_id`（390 确定性 preflight 孤儿/多归属硬失败 + 标量回填 + NOT NULL）、新 workspace unique/trace 表达式/unprocessed 索引（391–393）、旧 global unique/unprocessed 切换（394/395）、approval 幂等索引 workspace 化（396/397）；全 seam 切换：ingest conflict/processed、`lockKey(workspaceID,crID)`、runner checkpoint join、maturity.sql 七处 join、latestEvidence 直查 workspace；静态 contract 测试禁止不带 workspace 的 join/conflict | CR-2026-049 TASK-04/TASK-05（SDD §2）。迁移执行纪要：dev 库一次性 preflight 拦截遗留测试行 `CR-FIX-1`（两 workspace 同名）已删除后放行 | 2026-08-20 | 上游新增 385–397 迁移号时按《迁移编号冲突》一节顺延改名；maturity.sql 改后 `make sqlc`；`workspace_id` 是事件入账第一条件，上游在 cr_sync_event 新增列/索引时取并集。验证：`cd server && go test ./cmd/migrate/ -run 'TestCRSyncEventWorkspace|TestDriftFinding' -v` + `go test ./internal/governance/ -run 'TestCRSyncEventEvery|TestNoSpecTrace' -v` |
| 49 | `server/internal/governance/crsync.go`（trace 事件分支）+ `trace.go`（读服务，新）+ `trace_ingest_test.go`/`trace_test.go`（新）+ `server/internal/handler/trace.go`（新）+ `server/cmd/server/router.go`（1 处 AIFIRST：`/api/cr/*` 两 GET） | E4 追溯入账与读侧：`knownEventKinds` 增 trace；payload 2 MiB 上限 + canonical envelope 校验（spec_id/traceability/milestones/当前 CR 恰一段）；事务内 insert-or-load + FOR UPDATE + processed 标记，同键不同 payload `EVENT_IDEMPOTENCY_CONFLICT` 拒绝、committed processed 才 ack；trace 是 ledger-only 不触发 cr.status 转换。读侧：最新有效完整 snapshot 的全部 milestones 为展示集、`(cr,milestone)` 去重、baseline-imported 历史保序、事件赋时、语义哈希冲突标记、malformed 不泄漏 raw payload；spec-search owner 精确 + q ILIKE + keyset cursor | CR-2026-049 TASK-06/TASK-07（SDD §3.1/§3.5）。上游改 `HandleCREvents` 事件分派时保留 trace 专用分支（rejected 文件不删）；`/api/cr/*` 端点挂在上游 Auth 组 | 2026-08-20 | 上游改 crsync 事件分类时 `knownEventKinds` 补回 trace、`ingestTrace` 分支贴回；trace 表达式索引 392 依赖 `payload->>'spec_id'` 形状，上游改 payload 约定需同步。验证：`cd server && go test ./internal/governance/ -run 'TestTraceIngest|TestTraceService|TestProjectTimeline' -v`（真库） |
| 50 | `server/internal/commitprefix/`（新包：config.go + config_gen.go 生成产物 + gen/generate-prefixes.mjs + test）+ `server/internal/integrations/ghsnapshot/{client,commits}.go`+测试 + `server/internal/drift/{binding,finding,finding_repo,overview}.go`+测试 + `server/internal/service/commit_prefix_scan.go`+测试 + `server/internal/scheduler/jobs_commit_prefix.go`+测试 + `server/internal/handler/drift.go` + `server/cmd/server/{main,router}.go`（AIFIRST 注册/挂载） | E5 漂移检测：knowledge-base `dir-graph.yaml#repositories[].remote/commit_prefixes` 声明 → 严格生成器（行级硬失败、dirty guard、`--check`）→ `config_gen.go`（源 SHA 头）；workspace.repos 与声明 canonical 精确匹配 + GitHub installation Contents:Read 唯一解析（SSH canonicalize、token 只内存）；`commit_prefix_scan` job（1h、CatchUpLatestOnly、10m/15m/30s、retry 1/5/15m、scope=workspace）固定 HEAD B 分页到精确 cursor A、`wip:` 优先分类、每页 heartbeat、失败不推进 cursor、ON CONFLICT DO NOTHING；drift 三端点（overview 六态健康/keyset findings/PATCH CAS） | CR-2026-049 TASK-08~TASK-11（SDD §3.2~§3.6）。`commit_prefix_scan` 仅在 GitHub App 配置时注册；`config_gen.go` 是生成产物冲突丢弃重跑 `node server/internal/commitprefix/gen/generate-prefixes.mjs --source <kb-worktree>` | 2026-08-20 | 上游改 scheduler JobSpec 字段时按 SDD §3.2 锁定值贴回；`ghsnapshot` client 是上游既有文件，扩展走新文件 commits.go，上游重构 client 时 InstallationToken/ResolveRepositoryAccess/ListCommits 贴回。验证：`cd server && go test ./internal/service/ -run 'TestScanRepo|TestClassify' -v` + `go test ./internal/scheduler/ -run TestCommitPrefixScan -v` + `go test ./internal/drift/ ./internal/integrations/ghsnapshot/ -v` |
| 51 | 前端：`packages/core/{types/{trace,drift}.ts,{trace,drift}/queries.ts,api/schemas.ts,api/client.ts,paths/paths.ts,diagnostics/diagnostic-context.ts,package.json}` + `packages/views/{governance/spec-trace/,dashboard/drift/,search/search-command.tsx,dashboard/maturity/maturity-page.tsx,locales/*/search.json}` + `apps/web` 两页面 + `apps/desktop/src/renderer/src/routes.tsx` | 前端全链路：四组 zod + `parseWithFallback`（未知 enum 显示 unknown fallback、malformed 空 envelope）；五 client 方法；spec 时间线页（baseline-imported 保序、冲突标记、evidence 缺失显式、一跳 commit 链接）；全局搜索 Specs 分组（并行调用，选中进 `/{slug}/governance/specs/{specId}`）；maturity 漂移卡（六态与“无漂移”分离）+ drift 列表页（keyset 无限滚动 + PATCH 状态按钮终态禁用） | CR-2026-049 TASK-12（SDD §3.5/§3.7）。web/desktop 共用 `packages/views` 页面，不复制业务状态 | 2026-08-20 | 上游改 `search-command.tsx` 结果聚合时 Specs 分组贴回（`searchSpecs` 并行调用）；locale 四语言 `groups.specs` 键同步。验证：`pnpm -C packages/core vitest run api/drift-trace-schemas.test.ts paths` + `pnpm -C packages/views vitest run governance/spec-trace dashboard/drift search dashboard/maturity` |

## 纯配置约定（无代码改动，部署时执行）

| # | 项 | 口径 | 原因 |
|---|---|---|---|
| C1 | `MULTICA_CLOUD_FLEET_URL` / `MULTICA_FLEET_URL` | 永远留空 | 空值 → CloudPATVerifier 为 nil → `mcn_` 云节点令牌天然 401（`server/internal/middleware/auth.go`），本地执行路线不用云节点 |
| C2 | `DISABLE_WORKSPACE_CREATION` | 先建组织 workspace，随后置 `true` 并重启后端 | 单组织内部部署；上游 `.env.example` 自带此两阶段引导（#3433） |
| C3 | `ALLOWED_EMAIL_DOMAINS` | 部署时填公司邮箱域 | 限制内网注册来源；与 `ALLOW_SIGNUP` 组合为 AND 语义 |
| C4 | `.env` 行尾必须全程 LF | Windows 上不要用会改写行尾的工具碰 `.env` | 实测踩坑（2026-07-30）：`.env.example` 为 CRLF，`make selfhost` 复制生成的 `.env` 带 `\r`，Postgres 卷用"密码+`\r`"初始化；之后任何把行尾规范成 LF 的编辑（如 Git Bash 的 `sed -i`）都会造成 backend 与已初始化卷的密码不一致（SASL auth failed）。修复方式：数据可弃时 `docker-compose down -v` 重建卷 |
| C5 | 本机 agent CLI 需已独立 `claude /login`（daemon 干净环境要求已根修，见代码改动 #2） | 实测踩坑（2026-07-31，CR-2026-001 TASK-04）：Claude Code 桌面 App 的登录态不共享给命令行 `claude`，须一次性 `/login` | 同左 |
| C6 | 本机跑 Go DB 集成测试：`DATABASE_URL` 必须显式取真密码（`docker exec multica-postgres-1 sh -c 'echo -n "$POSTGRES_PASSWORD"'`），走 5433 端口转发（`multica-pg-forward-5433` socat 容器） | 两个坑（2026-07-31，T07 期实测）：① postgres 容器密码是 `.env` 生成的 48 位随机串，不是 `multica/multica` 默认值——各 TestMain 连不上时**静默跳过且 `go test` 仍打 `ok`**，必须 `-v` 看到 `--- PASS` 才算测过；② 宿主机 5432 会被 WSL relay（`wslrelay.exe` 抢 `::1:5432`）干扰导致认证错乱，避开共享端口 | 防"绿灯没测"假阳性 |
| C7 | 项目级 settings 键（`project.settings` JSONB）：`team_agent_queue_limit`（默认 50）、`team_agent_id`、`discussion_coordinator_agent_id` | 由 owner/admin 经 `PATCH /api/projects/{id}` 配置，缺省/非法值回落代码侧默认 | CR-2026-004 / 006 / 012；`handler/project.go` 有白名单校验，新增键要同步白名单 |

## 已知测试失败基线（上游既有，非本 fork 引入）

双周 rebase 后跑全量测试时，以下失败为已知基线，不计入"本次改动引入的回归"；若数量或名单变化才需要排查：

| 测试 | 包 | 确认方式 | 记录日期 |
|---|---|---|---|
| `TestTraecliBlockedArgsFiltering` / `TestQoderBackendInvokesACPFlagAndFiltersBlockedArgs` / `TestQoderFiltersRemoteMcpWhenInitializeDoesNotAdvertiseCapability` | `server/pkg/agent` | `git stash` 摘除本地全部改动后仍失败（2026-07-31，Windows + 本机装有 Qoder 的环境）；根因未诊断，疑与测试对本机环境的隐含假设有关 | 2026-07-31 |
| `TestNewAPIClient_LeftoverMarkerActionableError` 等 7 项 | `server/cmd/multica` | 未改动的 main 检出 A/B 复跑结果完全一致（2026-07-31，CR-2026-002 TASK-05 全量基线时发现）；多为 Windows 路径分隔符/本机环境假设 | 2026-07-31 |
| `TestCLIConfig_BackwardCompat_*` 等 4 项 | `server/internal/cli` | 同上 A/B 验证一致 | 2026-07-31 |
| `TestEnsureDaemonID_Persists` / `TestListRuntimeLocalSkills_*` 等 26 项 | `server/internal/daemon` | 未改动 trunk 检出 A/B 失败名单逐条一致（2026-07-31，T10 全量时发现）；典型根因：测试改 `HOME` 但 Windows 代码走 `USERPROFILE`（daemon.id 写进真实家目录路径失败）、本机已装 IDE 的 skill 根目录被真实发现。上游 Windows 环境假设缺陷，非 fork 引入 | 2026-07-31 |
| `builtin_skills` 相关 / `TestShortTaskIDMatchesDaemon` / `TestParseSkillArchive_RejectsUnsafeSkillMdPath` | `server/internal/service` 等 | CR-2026-010 TASK-01 期复核：CRLF 检出与 Windows 路径分隔符断言差异，与 claim/队列逻辑无关 | 2026-08-02 |
| gofmt：本机对上游 794 个文件报格式差异 | 全仓 | **根因是 CRLF**：Windows autocrlf 检出的上游 .go 文件字节级 ≠ gofmt 输出（LF），并非格式真差异（T06 期核实，修正 T05 时"工具链版本差异"的初判）。**fork 新增 .go 文件以 LF 写入且必须过 gofmt**；上游文件不动 | 2026-07-31 |
## 未做（防止误以为已做）

- **Tools Root 契约接入现存跨仓消费点**——CR-2026-028 只收敛 tools 仓的 active executable surfaces，不改 multica。当前 `server/internal/governance/gen/generate-{transitions,gate-nodes}.mjs` 默认猜测 sibling `../tools`，`approval_crosscheck_test.go` 与 `pkg/gitguard/gitguard_test.go` 也会扫描 sibling tools 路径；后续首次修改这些文件时应删除路径猜测：生成器要求显式 tools root，跨工具测试要求显式 `CRCTL_PATH` / rules path。生产环境现有 `MULTICA_CONTROLLED_SHELL_RULES=<绝对路径>` 属于安装时物化，继续复用，不在 multica 内新增 resolver。
- **Pipeline Runner**——CR-2026-045 已交付 Runner Core（#31，architecture-design 五节点 `Reconcile`）；仍未做的是**其它 pipeline 的 Runner**（当前只有 architecture-design 有执行器）与 crctl `--caller` 到 Multica 用户的身份桥接（#15 的 `canApprove` 限制）。
- **crctl `--caller` 到 Multica 用户的身份桥接**——因此 `canApprove`（#15）只能按工作区 owner/admin 判定，无法按 `cr.owners` 的角色判定（`canApprove` 注释内有 `ponytail:` 标注）。
- **controlled-shell L1 层（`--disallowedTools Bash`）**——agent 的 `permission.bash` 平台不持久化、无可信键，只落地了 L2 PATH shim + L3 IDE hooks（#8）。L2 用绝对路径可绕过，已在文档明示。
- **多知识库的 workspace→remote 映射表**——server 模式 reconcile（#10）当前假定单一知识库远端。
- **技能选择器在项目群聊输入区的排除**——CR-2026-012 §7 另议，未做。

> 已删除的旧「未做」条目（避免二次误导）：controlled-shell/gitguard 下沉 daemon 已由 #8 完成；CR 投影表与签名审批已由 #3/#7 完成。
| 48 | 迁移 `380_skill_visibility`～`384_skill_appeal_activity_index`（新，各 up/down 成对）+ `server/cmd/migrate/main.go`（concurrentIndexCleanups 补 382/383/384）+ `pkg/db/queries/{skill_market.sql(新),skill.sql,workspace_delete.sql}`（及 sqlc 生成物 models.go/skill.sql.go/skill_market.sql.go/workspace_delete.sql.go）+ `pkg/redact/redact.go` + `internal/skill/{publish_gate.go(新),frontmatter.go}` + `internal/handler/{daemon.go,skill.go,skill_appeal.go(新),skill_market.go(新),skill_publish.go(新),skill_create.go,runtime_local_skills.go}` + `cmd/server/router.go` + 前端 `packages/core/{types/agent.ts,api/client.ts,types/index.ts}`、`packages/views/skills/components/{market-page.tsx,skill-market-card.tsx(新),skill-detail-page.tsx,index.ts}`、`packages/views/skills/lib/skill-metadata.ts(新)`、`packages/views/locales/*/skills.json`、`apps/web/app/[workspaceSlug]/(dashboard)/skills/market/page.tsx`、`apps/desktop/src/renderer/src/routes.tsx` | 内部 Skill Market（E6）：`skill` 加 visibility(仅 private/org)/version(纯展示)/owner_actor 三列；`skill_usage_event` append-only 遥测（workspace_id 租户键、无 FK），claim 路径 best-effort 写入（`TaskCompleteRequest`/`sanitizeTaskCompleteRequest` 零改动）；发布门禁 `EvaluatePublish` 纯函数（frontmatter/owner/元数据卡四字段/敏感扫描/personal_path 第 17 条/protectedPaths 警告/申诉放行），private→org 与 org 内容更新（含 runtime-local 覆盖导入）同门禁；申诉账本复用 activity_log（appeal_id=内容哈希绑定，由门禁下发、申诉/决定按同一 id 记账；决定取最近一条 approved/rejected，故驳回可撤销此前放行；384 部分索引照 089 形制）；`GET /api/skills/market` 按 workspace 聚合去重（completed 过滤）；workspace 删除图显式清理遥测行 | CR-2026-048 TASK-01~10；P3 §3/§5.1 CR-B/§5.2 E6 | 2026-08-20 | 上游新增 380–384 迁移号按《迁移编号冲突》顺延并同步两个 cleanup map 键名；上游改 `buildClaimedTaskResponse` useSkillRefs 分支时保留遥测循环；上游改 `UpdateSkill`/`overwriteSkillWithFiles` 时保留门禁挂点；sqlc 生成物一律 `make sqlc` 重生。验证：`cd server && go test ./internal/skill/ -count=1`、`go test ./internal/handler/ -run 'TestUpdateSkill|TestAppealFlow|TestGetSkillMarket|TestClaimTaskByRuntimeWritesSkillUsageTelemetry' -count=1 -v`（真库）、`go test ./cmd/migrate/ -run Concurrent -count=1`、`go build ./...`、`go vet ./internal/handler/ ./internal/skill/ ./pkg/redact/ ./pkg/db/generated/ ./cmd/migrate/`；前端 `packages/core`+`packages/views` typecheck |
